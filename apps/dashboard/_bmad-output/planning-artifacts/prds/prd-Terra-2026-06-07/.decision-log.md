# Decision Log — Terra Tool 1 PRD

Canonical memory and audit trail for this PRD run. Every decision, change, and
override is recorded here as the conversation unfolds.

## Run metadata
- Intent: Create
- Started: 2026-06-07
- Facilitator: bmad-prd
- Authoritative source: `_bmad-output/project-context.md` (Critical Don't-Miss Rules win on data-model conflicts)
- Working mode: (TBD — Discovery)
- Entry point: (TBD — Discovery)

## Decisions
- 2026-06-07 — Workspace bound at `planning-artifacts/prds/prd-Terra-2026-06-07/`. Intent: Create. Confirmed no prior in-progress runs.
- 2026-06-07 — Background landscape research completed; saved to `research-landscape.md`. Four flags carried into discovery: (1) AG TOU rate peak is 5–8pm year-round, distinct from the 4–9pm PDP/DR event window (context file / principles use the 4–9pm framing for curtailment — correct for DR, not for the base rate peak); (2) Bayou's docs claim multi-account-per-login since July 2023, contradicting the saved "one account/login (verified)" memory — re-verify against the live PG&E flow; (3) PG&E HFP ag automation incentive ($160/kW) is CLOSED as of 3/2026 (cap exhausted), pricing pilot runs to 2027; (4) one rate-plan switch per meter per 12 months is a hard product constraint on rate-optimization recs.

### Brain dump captured (2026-06-07)
- **Batth real-data shape documented** → `addendum.md` §A. Authoritative real-data source is the grower master spreadsheet (1 sheet, 183 meters). Hierarchy entity → account → meter; rollup adds ranch. Real counts: 7 billing-name variants (dedupe to true legal entities), 57 accounts, 183 meters, ~83 HAGC, 27 Legacy-flagged, ~56 solar, 37 ranches. Full per-meter field inventory captured.
- **CORRECTION 1 RESOLVED (5–8 vs 4–9):** accepted, bake in. 5–8pm = rate TOU peak (demand-charge math); 4–9pm = DR event window. Separate clocks in math + copy. Build "solar collapses before 5–8pm peak → net-zero-on-energy still owes demand charge" as a surfaced retrospective insight.
- **CORRECTION 2 RESOLVED (Bayou):** mark "re-verify against live PG&E flow"; do not overwrite the verified one-account test on a changelog, do not treat one-account as hard fact. Real-data path stays spreadsheet / Green Button / formal Share My Data regardless — Batth's 57 accounts span multiple legal entities and likely multiple PG&E logins, which multi-account-per-login would not cover.
- **CONSTRAINT — hard deadline:** user leaves ~July 20, 2026 for a ~5-month field deployment (~6-week build runway). Rebuild must be demoable and ideally converting Batth before then, and structured so Kamran can carry it during the absence. Scope strictly to this runway.
- **CONSTRAINT — dual audience:** (a) convert the Batth LOI by showing Gagan his own real operation; (b) double as live proof-of-product in investor meetings. Both need a clean demo, not just correct math.
- **DATA-MODEL implications:** explicit array → benefiting-meter relationships (not flat meters); entity → account → ranch → meter rollup with first-class meter/pump health (`Status`: GOOD/BAD/NEW WELL/OLD); `Actual owner` distinct from `Billing Name`; surface `Existing descriptor` (real grower name) not synthetic seed names.

### Data-path correction (2026-06-07) — SUPERSEDES prior "Bayou/Green Button/Share My Data as real path"
- **Bayou PARKED, not primary.** Responded but not functional for Batth's accounts yet.
- **v1 real ingestion = PG&E bill PDFs.** Batth's assistant pulls PG&E "download my data" (spans all accounts), sends PDFs; v1 ingests them. PDF is the bridge; Green Button/Bayou the destination. Captured in `addendum.md` §C.
- **Two layers joined on `SA ID`:** master spreadsheet = inventory layer (all 183 meters day one, by entity/ranch, real names); parsed PDF = billing layer attached per service agreement via `SA ID`.
- **One PDF → many service agreements from the start** (accounts lopsided: 45 meters, 36 meters). Not one-bill-one-meter.
- **Normalize PDF output AND future Bayou adapter to ONE canonical billing shape** — nothing downstream changes when Bayou comes online.
- **Reconciliation guardrail (trust surface):** a number renders only if extracted line items reconcile to the bill's printed total within one cent; else withhold + flag "needs review." Prove the parser on one account before bulk.
- **Launch reality:** full inventory day one; billing partial at launch (parser starts on one account, expands). Dashboard must be correct + legible with partial billing coverage.

### Vision — moment of truth RESOLVED (2026-06-07)
- The conversion moment is the **legibility win, anchored by one number.** First 10 seconds: Gagan sees all 183 meters across his 6 entities and ~36 ranches in one correct place, with his real pump names, and **total PG&E spend across everything as the single hero number.**
- **NOT money-left-on-the-table first** — legible-before-predictive: he won't trust a savings claim from a tool he just opened, and billing is partial at launch while inventory is complete day one.
- **Overpayment is the second beat,** surfaced once he believes the picture.
- _Open: ranch count stated as 37 (brain dump) then 36 (here) — confirm against the real sheet._

### Audience / demo surfaces RESOLVED (2026-06-07)
- **Investor is a viewer, not a user. No investor-specific build in v1.**
- **Two demo surfaces, both already supported by the model — zero new build:**
  - **Conversion demo = Gagan's real screen** (`isDemo:false`, no badge). Real data, billing partial at the June window.
  - **Investor demo = the badged representative seed** (`isDemo:true`, "Representative data" badge; real-supersedes-demo resolution). It tells the COMPLETE money story the live screen can't yet (real billing partial), and sidesteps showing a real grower's financials to investors.
- **Content requirement on the seed:** the representative seed must present a complete, believable money story (all levers populated) so it carries the investor room. This is "polish the existing seed," not new build.
- **NON-GOAL v1:** no anonymized-real mode (build we can't afford). If a room can't see live customer data, the badged seed covers it.
- Kamran is the caretaker/operator carrying the product during the absence (onboarding, demos), not a distinct in-product user role in v1.

### Target User RESOLVED (2026-06-07) — §2 drafted into prd.md
- **JTBD confirmed verbatim** (5 jobs), no changes.
- **UJ-1** = first reveal (conversion). **UJ-2** = the bill landing (the recurring loop — Gagan reopens when a new bill posts, the one moment he already cares about PG&E). **UJ-3** = overpayment finding (recommendation grammar).
- **Close-the-loop IS IN v1, lightweight:** predicted total vs actual posted total, "did the tool call it right." Cheap — a surfaced diff over data already ingested, not a new engine. **Scope guardrail: v1 = "predicted vs actual, did we call it," NOT variance attribution.** The "why was it off by $400" breakdown is a later beat.
- **Proactive spike alerts (Brex-style) OUT of v1.** Reason: PG&E lags ~a day and launch billing is partial → alerting on incomplete data risks false alarms in front of the first real customer. Revisit once billing is complete and Bayou is live.
- **Non-users (v1):** investor (viewer of badged seed), farm employees/irrigators doing operational scheduling, PG&E/utility staff, Kamran-as-product-role.

### Features stage — `docs/product-ux-research.md` read (2026-06-07)
Research confirms the cluster map (Stripe card→chart→table→drawer home; Wexus feature set as the bar; staged recs = Stage 1 dashboard, Stage 2 Brain). Filters applied so the research does NOT override the Critical Don't-Miss Rules:
- **Typography: the doc says "Inter" (Linear recipe) repeatedly — REJECTED.** Project design system forbids Inter; Terra uses Fraunces / Hanken Grotesk / JetBrains Mono. Carry the *craft bar* (8px scale, tabular figures, restraint, sub-second nav), not the font.
- **Real-time alerting NOT v1.** Wexus's 15-min-interval demand tracking + 90-minute peak/demand SMS alerts depend on IoT hardware + live data. Critical Rule: planner-not-live-meter, PG&E lags ~a day, never promise spike detection; spike alerts already ruled OUT for v1. So Wexus features #5 (90-min alerts) and real-time anomaly alerting are NOT v1. Also: never expose "15-minute interval" jargon on the surface.
- **Agentic Brain (Part 2: ⌘K palette, sidebar copilot, propose→approve→execute, command palette) = Stage 2, NOT v1.** v1 DISPLAYS recommendations (recommendation grammar, shaped to be executable later). Research's own staged plan agrees: Stage 1 = ship the dashboard to a Wexus-beating bar.
- **Remote pump shut-off = OUT** (no hardware; agent acts later).
- **Predictive Irrigation Cost Calculator (ET-based forecasting) = OUT/minimal v1** — retrospective-before-predictive; precision irrigation is lever 6 (future tool).
- **Map view = OPEN scoping decision for cluster B** (research strongly endorses it: Wexus pattern, lat/long present, growers think spatially — but it's real build on the runway).
- **Traffic-light green/amber/red** (research strongly endorses for "is there a problem") now informs the **open visual-system decision (b)** in project-context (severity info/watch/act had no agreed color). Direction: earn amber/red for watch/act rather than carry it on the restrained money-green alone. Architect settles exact palette.
- Borrow from Wexus, deliver at Linear/Stripe craft, beat with the AI layer their human-engineer model can't scale (per research TL;DR).

### Cluster A — Data Foundation RESOLVED (2026-06-07) — §3 Glossary + §4.1 FR-1..FR-6 drafted into prd.md
Verified against the real Batth account file:
- **Bills are SCANS** — 101-page image-only PDF, no text layer, bilevel 200 DPI. Extraction = **vision/LLM → strict JSON**, NOT text parsing. OCR errors expected; the one-cent reconciliation guardrail catches them.
- **Parser classifies page type before extracting:** payment-confirmation / account summary / per-SA summary list / per-SA charge detail / per-SA NEM reconciliation. NEM meters show negative usage.
- **Per SA the bill carries:** printed rate schedule name (read-the-rate feasible; cross-check vs sheet), meter # + Pump ID (join via SA ID), TOU energy split (Net Peak / Part-Peak / Off-Peak), demand charge + NBCs on detail pages. **Handle two-tier (current) AND three-tier (legacy AG5B) TOU.**
- **Multi-period from one export** (saw 7 monthly NEM rows) → YoY feasible; canonical billing shape is multi-period; v1 not blocked on full history.
- **NOT extractable:** 15-min interval data, demand-peak timestamp. **v1 demand analysis is cycle-level** ("$X demand on a Y kW peak"), no intra-day curve.
- **Scale:** 1 account = 101 pages; 57 accounts = thousands of pages = a real pipeline. **v1 proves the parser on ONE account; bulk explicitly DEFERRED.**
- FRs: FR-1 inventory import, FR-2 scanned-bill extraction, FR-3 NEM reconciliation extraction, FR-4 canonical billing model + SA-ID join, FR-5 reconciliation guardrail, FR-6 partial-billing coverage. Detail captured in `addendum.md` §C.1.

### Cluster B — Dashboard (the hero) RESOLVED (2026-06-07) — §4.2 FR-7..FR-12 drafted into prd.md
- **Calendar CONFIRMED demoted** into the billing-cycle-timing lever (§4.3), NOT home. Home = cards → chart → table → drawer.
- **FR-7 KPI cards:** hero = total PG&E spend (scoped to covered period, coverage indicator beside it) + demand-charge exposure + biggest cost mover (gated on ≥2 covered periods, hide gracefully). Keep coverage indicator ("142 of 183 meters loaded") — reads as thoroughness, shows 100% on investor seed. **DEFER** savings-captured-YTD + projected-month-end (→ §4.4 D). **HOLD (Critical Rule):** NO overpayment/savings hero card — "money left on the table" is the second beat, lives in the recommendation feed (§4.3 C), not home.
- **FR-9 Meter table = P0** (the trust surface). **FR-12 Map = P1, lightweight read-only** (geotagged pins, traffic-light by status/cost, tap → drawer); inventory-only so it renders day one + on the partial-billing seed = strongest investor-seed visual. **Map is the explicit FIRST scope cut if cluster A's parser eats the runway; table takes precedence.**
- **Mobile resolution:** mobile core = hero + KPIs + simplified sortable list + drawer (+ map if it survives); full dense multi-column table = tablet/desktop progressive enhancement. **Reality check: both six-week demos (Gagan conversion + investor) run on a LAPTOP → make desktop/tablet land hard with a clean responsive phone view; keep mobile-first as discipline so nothing breaks on a phone, but desktop is the surface in the room.** → carry to Cross-Cutting NFRs.
- **Traffic-light color** (watch/act earn amber/red) confirmed as direction for cards/table/map; settles open visual decision (b) pending Architect.

### Cluster C — Recommendations + levers RESOLVED (2026-06-07) — §4.3 FR-13..FR-18 drafted into prd.md
- **FR-13 Recommendation feed:** grammar, secondary to dashboard (feed + per-meter drawer, never a home hero card), propose-then-approve display, traces to visible data, v1 displays (agent executes later). A finding with no impactUsd and no impactNote is not shown.
- **FR-14 Rate optimization = the one fully-real lever.** Lead with 27 legacy meters. Mechanics: **option (a)** — dated, versioned PG&E ag tariff fixture (per schedule: customer charge, TOU energy by season, demand charge, limiter), apply meter's own TOU usage + billed demand to each eligible schedule. **Back-test gate (the honesty mechanism):** recompute the meter's CURRENT bill from the fixture+usage; only if it reconciles within tolerance do alternative-schedule savings render (and show rates used + effective date so Gagan can verify); if it fails → "needs review" + fall back to (c) qualitative legacy→current. **(b) REJECTED** (one meter's blended effective rate isn't transferable → misleading). **Fixture scope bounded** to Batth's schedules + current-equivalents (AG5B/5C/4C, HAGC, HAGA1/2/B, AG-A/B/C), NOT all 17 ag schedules. Respect 35 kW eligibility + once-per-12-months switch.
- **FR-15 Solar/NEM single insight** — SCOPE FIX: "net-zero on energy, still owes demand" renders ONLY for solar meters on a demand-carrying schedule (AG-C family); never on a solar meter with no demand charge (would be wrong). Drawer NEM section + feed item.
- **FR-16 Billing-cycle timing** — lightweight calendar from serial + 2026 meter-read fixture; the demoted hook, kept small.
- **FR-17 Pump health flag** — flag BAD-status pumps (master `Status`) in table+drawer. **NO kWh-per-gallon math** (have GPM, lack runtime/volume → would be invented → breaks no-fake-numbers). Efficiency lever deferred; health flag in.
- **FR-18 DR enrollment** — DEFERRED as a rec (no defensible $ → fails grammar; Batth already on PDP). Drop the static "you may qualify" pointer. At most display enrollment status as legible info from the bill, no recommendation.
- **Lever 6 precision irrigation OUT; coincident-peak staggering OUT/unsurfaced** (keep code, demote). Confirmed.

### Clusters D + E RESOLVED (2026-06-07) — §4.4 + §4.5 (FR-19..FR-21) drafted into prd.md
- **D close-the-loop = (i) + (iii), skip (ii).**
  - **FR-19 (i) = ACCURACY, not prediction (relabel).** Recompute posted bill from tariff fixture + meter's own usage, match it. Label exactly as a **verification badge**: "Terra independently calculated this bill from the rates and your usage and matched it to the cent." NEVER dress as "we predicted your bill" (a sophisticated operator would call it circular). Matching ground truth is what licenses FR-14's alternative-schedule numbers.
  - **FR-20 (iii) = the real close-the-loop:** accepted rec → predicted impact vs realized number on next bill via FR-13 `result`. Diff only, not variance attribution.
  - **Demo framing:** FR-19 = live trust signal (completed match every cycle); FR-20 reads "pending result" until a bill posts after a rec is accepted. Don't script the demo as if a loop already closed.
  - (ii) forward projection DEFERRED (no projection model on this runway).
- **E data-in = concierge/admin import; NO grower upload UI in v1.**
  - **FR-21:** spreadsheet + PDFs via dev/admin import (script-level fine; Jay+Kamran technical). One account proven first. No self-serve onboarding.
  - **Keep Bayou live-connect flow DORMANT (not removed)** — future path once Bayou works, targets the canonical billing shape (cluster-A seam).
  - **FOUNDER-DEPENDENCY FLAG → Non-Goals (deliberate deferral, not accident):** recurring loop needs new monthly PDFs over time; Jay gone ~July 20 for ~5 mo with Kamran handoff. Concierge-only ingest stalls the product when Jay is unavailable. A **minimal Kamran/Jorge-runnable ingest path = the explicit FIRST fast-follow after v1.**

### §5 Non-Goals + §6 MVP Scope drafted (2026-06-07)
- Assembled from the logged deferrals across clusters A–E (no new interview). In/Out lists in prd.md §6; broader "not building/becoming X" in §5. Founder-dependency fast-follow flagged `[NOTE FOR PM]` in §6.2. Pending user review.
- Remaining sections: §7 Success Metrics (needs user targets), Cross-Cutting NFRs, §8 Open Questions, §9 Assumptions Index, then Finalize.

### Success Metrics LOCKED + wrap-up drafted (2026-06-07)
- **SM-1 Batth converts** (signs a paying pilot) + **SM-2 investor signal** (demo advances the raise) — both BINARY, no invented targets.
- **SM-3** reconciliation = 100% of displayed figures tie to the cent on the proven account ("needs review" otherwise). **SM-4** = each of 27 legacy meters carries a back-test-passing alt-schedule finding with a checkable $ number. **SM-5** = both demos run end to end, zero wrong numbers, no stall.
- **SM-C1** correctness>polish, **SM-C2** one real lever>six shallow, **SM-C3** "needs review">fake coverage. Kept as written.
- Drafted into prd.md: §0 Document Purpose, §7 Cross-Cutting NFRs, §8 Success Metrics, §9 Open Questions, §10 Assumptions Index. Founder-handoff reality is in §5 Non-Goals + §6.2 + FR-21 note. Full PRD assembled (FR-1..21). Frontmatter still `status: draft` pending Finalize (reviewer gate + polish + status:final).

### FINALIZE — reviewer gate + triage (2026-06-08)
Ran 4 parallel passes: input reconciliation vs `project-context.md` (`reconcile-project-context.md`) and vs `product-ux-research.md` (`reconcile-product-ux-research.md`); rubric walker (`review-rubric.md`: STRONG, 1 high/5 med/4 low, no criticals); adversarial (`review-adversarial.md`: HIGH-risk verdict, all on the reconciliation/back-test seam). Fixes applied to prd.md:
- **FR-2 + FR-5 (reconciliation target):** extraction must capture EVERY line composing the SA's printed total (customer/min charge, taxes, riders, credits), and reconciliation is against the printed total (SA-level + account-level), not a partial subtotal. Closes the "weak subtotal → SM-3 falsely true / never reconciles" hole.
- **FR-4 (join identity):** SA-ID join is identity-checked (extracted meter#/Pump ID must match inventory row) → mismatch = "needs review". Stops a cent-perfect figure landing on the wrong meter.
- **FR-14 (back-test tolerance):** RESOLVED as a defined small **% band** (not one cent; riders/baseline outside fixture), calibrated in build; rate savings render as labeled **estimates**. Distinct from FR-5/FR-19 cent-exactness (those check ground truth; back-test validates a model).
- **SM-4 SOFTENED (deviation from locked — flag to user):** from "each of 27 legacy meters carries a $ finding" → "every legacy meter whose back-test PASSES carries an estimated-savings finding; failures fall back to qualitative." The original was likely unachievable (would force loose tolerance → dishonest numbers).
- **FR-7:** sparkline/delta degrade gracefully with a single covered period (Batth's real screen at launch).
- **FR-9:** "demand $ / peak kW" → "demand charge ($)" — removes the never-kW surface-copy violation.
- **FR-22 added:** one-click CSV export (was a genuine drop; Wexus/CLAUDE.md/CFO benchmark). SGMA energy-to-water export → §6.2 out-of-scope.
- **Glossary:** added Crop, Person, Connection (first-class shared entities were dropped).
- **§6.3 added (risk):** cut-order if parser overruns (protect FR-1/2/4/5/7/9/14); extraction proof milestone ~week 2 + hand-verified-extraction fallback; demo coverage-asymmetry framing (seed 100% vs Batth partial; legibility rides on complete inventory, hero spend is covered-scope).
- **§7:** comprehension bar (<~10s), motion stagger 60–100ms/no-bounce, "Representative data" badge + never-merge invariant.
- **OQ-2 resolved-in-principle** (band calibration is the remaining build task); **OQ-8 added** (Vercel fixture trap for the tariff + meter-read fixtures).
- Reconciliation items deferred (non-blocking): retrospective solar-value view (optional enhancement, narrowed for runway), three-views "flip the dataset" unification (low priority). Competitor "~40%" not cited in PRD body → no action.

### Editorial polish + CLOSE (2026-06-08)
- Editorial pass (structure + prose): FR-1..22 continuity clean, cross-refs resolve. Applied: fixed "build effort competing" grammar; un-stranded SM-4 "Validates FR-14"; added Glossary "Representative seed" (canonical noun); trimmed restated form-factor NFR and FR-13 trailing clause. Defensible repetition (single-account scope) left intact.
- **No unresolved phase-blockers.** Remaining open items are non-blocking deferrals with owners/triggers (back-test band = build calibration; tariff sourcing; Bayou re-verify; history depth; visual-system = Architect; working title; Vercel fixture trap = Architect).
- **Two finalize deviations from user-locked items — flagged to user for sign-off:** (1) SM-4 softened from "all 27 legacy meters" to "every legacy meter whose back-test passes"; (2) FR-14 rate savings reframed as labeled *estimates* (cent-exactness reserved for FR-5/FR-19 ground-truth checks). Both preserve intent (defensible, checkable) while removing an over-promise the reviewers flagged.
- **prd.md frontmatter set `status: final`, `updated: 2026-06-08`.** PRD FINALIZED. Companions: addendum.md, research-landscape.md, reconcile-*.md (2), review-rubric.md, review-adversarial.md.

### Brand green RESOLVED (2026-06-08, post-finalize) — user decision
- **Dominant brand green = `#2fa84f`** (user-specified). Resolves project-context open visual-system decision (a). Supersedes deep-forest `#1F3D2B`–`#2D4A2D` and marketing `#1C7A2B`.
- Changed: `src/app/globals.css` (`--green: #2fa336` → `#2fa84f`); `CLAUDE.md` lines 56 + 67; `_bmad-output/project-context.md` decision (a) marked RESOLVED; `spec.md` line 33; `prd.md` §7 (anchored hex) + Open Question 6 (resolved half).
- LEFT / flag for design: the "brighter-green accent" prose (CLAUDE.md:69, project-context:150) reads oddly against a bright main green, and the live tokens actually use gold + sky as accents (not a brighter green) — accent direction still unsettled. Derived green shades (`--green-deep #1c7a2b`, `--green-hover`, `--green-tint`) not re-derived (new main ≈ old, so they still hold); revisit if a true re-palette is wanted.

### Typeface RESOLVED — all Helvetica (2026-06-08) — user decision ("make it all halvetica", delegated details)
- **One typeface across display, body, and data: Helvetica.** Reverses the prior "never system fonts / Fraunces (display) + Hanken Grotesk (body) + JetBrains Mono (data)" anti-slop system. User asked for "all" and delegated the how ("whatever you think is best").
- Implemented as a **real Helvetica stack** `"Helvetica Neue", Helvetica, Arial, sans-serif` (renders as Helvetica Neue on the demo Macs, Arial only as last-resort fallback), NOT a lazy `Arial`/`system-ui` default. Hierarchy carried by weight + size; **tabular figures kept** on all numbers (.tnum/.figure). Dropped the Fraunces hero-number serif.
- Changed — code: `src/app/layout.tsx` (removed all `next/font/google` imports + the three font consts + the variable classNames) and `src/app/globals.css` (added `--font-helvetica` token; `--font-sans/--font-mono/--font-display` all → it; body, `.font-display`, `.eyebrow`, `.figure` font-families → it; `.font-display`/`.figure` weight bumped to 700 to carry display emphasis without the serif). Docs: `CLAUDE.md` (2 type rules), `_bmad-output/project-context.md` typography, `prd.md` §7 + §6.1, `spec.md` Type section + the "design tokens in place" status line.
- `npm run lint` passes clean. Reversible knob if wanted: keep Fraunces on hero `.figure` numbers only (the "money is the largest element" identity) while body/UI stay Helvetica.

### Typeface CHANGED again — Inter (2026-06-08, supersedes Helvetica above) — user decision
- **One typeface across display, body, and data: Inter**, loaded via `next/font/google` in `layout.tsx` (`--font-inter` → `--font-sans` / `--font-mono` / `--font-display`). Reverses the all-Helvetica decision. (Note: Inter was the original anti-slop system's #1 forbidden font; the "never Inter / never system fonts" rule is fully retired as of this change.)
- Changed — code: `layout.tsx` (added Inter via next/font, dropped the no-webfont Helvetica setup; preserved the user's updated metadata description), `globals.css` (removed `--font-helvetica` token; tokens + body/.font-display/.eyebrow/.figure now reference the Inter-backed `--font-sans`/`--font-display`; tabular figures + display weights 700 kept). Docs: `CLAUDE.md` (2 rules), `project-context.md` typography, `prd.md` §7 + §6.1, `spec.md` Type section + status line — all Helvetica → Inter.
- `npm run lint` passes clean; verified zero remaining `helvetica` / `--font-helvetica` references in src or governing docs.
