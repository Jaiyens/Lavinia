# Decision Log — PRD: Almond, Terra's generative operator (the wedge)

Canonical memory + audit trail for this PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

- **Run:** `prd-Almond-2026-06-17`
- **Facilitator skill:** bmad-prd
- **User:** Jaiyen
- **Intent:** Create (new, focused PRD). Almond exists today as a read-only assistant (Epic 6, done); this PRD gives it agentic "do" capability (navigate + generate) as a **generative operator** — an addition to the dashboard today, a latent wedge as growers grow AI-native (see D10). Kept as a standalone PRD, not folded into `prd-Terra-2026-06-07` (Tool 1 energy dashboard). User to correct if they want it merged.

## Decisions

| # | Date | Decision | Source |
|---|------|----------|--------|
| D1 | 2026-06-17 | **Working mode: Fast path.** Batch gaps, draft full PRD with `[ASSUMPTION]` tags, iterate. | User (Q) |
| D2 | 2026-06-17 | **Wedge meaning: "assistant that now acts" — dashboard-first.** Almond stays the in-app rail but gains the power to open pages, run skills, and produce exports. NOT a chat-first front door; NOT framed only as a GTM hook. The dashboard stays the hero; Almond is the operator that drives it. | User (Q) |
| D3 | 2026-06-17 | **Action scope: navigate + generate, read-only on data.** Almond opens any page/drawer/filter and produces files, but never mutates farm or utility data (no resolving findings, no rate changes). Extends Terra's existing "display, never execute" law to Almond. Write-actions and utility-side actions are explicitly out of v1. | User (Q) |
| D4 | 2026-06-17 | **v1 skills: CSV/Excel export, PDF report, open & navigate pages.** PPTX is a future skill, not v1. The CSV/Excel skill EXTENDS the existing export (`src/lib/dashboard/csv.ts` `metersCsv` + the meter-table export), does not reinvent it. | User (Q) |
| D5 | 2026-06-17 | **Framed as an extensible skill system** so future skills (PPTX, email delivery, scheduled reports) plug in without reworking Almond. | Facilitator (confirmed by D4 framing) |
| D6 | 2026-06-17 | **Open behavior: Almond drives the screen.** When Almond opens a meter/filter/map, the main view jumps there by reusing the app's existing URL state (`?meter=`, `lens`, `entity`/`ranch`/`rate`); Almond notes what it did as an action chip. Non-destructive, so "back/undo" is just navigation. | User (Q) |
| D7 | 2026-06-17 | **Export delivery: download to device + save to a Reports area.** Email delivery is NOT in v1 (future skill). Saving requires a persisted, farm-scoped Reports surface + file storage. | User (Q) |
| D8 | 2026-06-17 | **Exports are request-driven / generative, not a rigid template.** The PDF (and spreadsheet) content is "whatever the farmer asks for" — Almond composes the artifact from the grower's request, grounded in real data, with sensible defaults. No fixed single-template report. | User (Q) |
| D9 | 2026-06-17 | **Tech grounding (→ addendum):** pure-JS server-side generation (`exceljs`/`pdfkit` or `@react-pdf`/`pptxgenjs`), NO headless Chromium — avoids the Vercel bundle-size/cold-start tax. Stored files via blob storage with scoped, expiring URLs. Model boundary stays injected (stub offline / Vercel AI Gateway live), zero external calls in CI. | Research subagent + facilitator |
| D10 | 2026-06-17 | **Almond is NOT the wedge — it is an addition.** Today the dashboard is what growers want/need (data upfront; the bill-due calendar, the #1 farmer ask, is built and IS the home lens). Almond becomes a large wedge LATER as growers grow AI-native. Terra's REAL wedge/moat is **physical↔numerical (camera) data fusion** — out of scope here but frames Almond's role. Title + §1 vision rewritten; "wedge" language removed throughout. | User (correction) |
| D11 | 2026-06-17 | **FR21 confirmed** (clear, discoverable entry). **FR22 reframed: gentle, progressive, never overbearing** — non-AI-native growers are easily confused/put off; UX friendliness is a hard requirement, not polish. §5.6 retitled "Surfacing Almond (gently)". | User |
| D12 | 2026-06-17 | **Data-first export cut.** v1 exports lead with the data growers trust (meter table, bill-due schedule); findings/recommendation exports deferred pending farmer validation. Reflects the broader product truth that farmers want data upfront, not findings/recs first (consistent with hero-not-money-loudest). | User |
| D13 | 2026-06-17 | **Storage decided: keep Neon Postgres; report files in Vercel Blob (private, scoped/expiring). Supabase migration REJECTED** (second platform + re-do auth for ~zero gain; the need is file storage, not a DB swap). Perf NFR + success metrics confirmed (direction; numbers open). New NFR: Almond stays native to the constantly-changing dashboard via the single canonical surface contract. | User asked → facilitator decided |
| D14 | 2026-06-17 | **Farmer validation is the decisive open item** — put the addition thesis, gentle surfacing, and data-first cut in front of a real grower before heavy build (only a farmer knows what a farmer wants). | User |

## Research grounding (digest captured)

- **Notion's agent takes write actions but gates every create/update/delete/send behind a confirm-with-preview, and inherits the user's permissions.** Because Terra chose read-only v1 (D3), we sidestep the confirmation-risk surface entirely; we keep only the lightweight "here's what I'm about to make" preview for heavier artifacts.
- Notion model = "AI teammate," assisted autonomy, undo as a first-class affordance, custom-agents = scheduled background workflows (our "future scheduled reports").
- Agent-as-front-door comparables (Glean, Sierra, Decagon) all go chat-first; Terra deliberately does NOT (D2) — lower risk, dashboard stays the hero.
- Export-in-agents pattern: pure-JS libs cover CSV/XLSX/PPTX; Chromium only needed for pixel-faithful HTML→PDF and is the serverless-gotcha path — avoid it.

## Open items (tracked; resolved at Finalize)

- **Retention / quota** for saved Reports at Batth scale (storage itself decided — Neon + Vercel Blob, D13).
- **Gentle-surfacing design** (FR21–FR22 rail entry + onboarding nudge wording) — direction confirmed (D11), design detail open.
- **Numeric targets** — activation metric + generation latency (direction approved D13, numbers open).
- **Farmer validation (D14)** — the decisive open item; validate the addition thesis, gentle surfacing, and data-first export cut with a real grower before heavy build.
