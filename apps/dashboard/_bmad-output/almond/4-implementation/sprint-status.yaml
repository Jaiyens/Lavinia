# =============================================================================
# Almond - Terra's Generative Operator (Epics 7-10) - Sprint Plan
# =============================================================================
#
# This file tracks every epic and story from
#   _bmad-output/almond/3-solutioning/epics.md
# against the file-system tracking system. Statuses move left to right:
#   backlog -> ready-for-dev -> in-progress -> review -> done
#
# This is an EXTENSION effort. It continues the single Terra epic space
# (Epics 1-6 shipped; Epic 6 = the read-only Almond) and is tracked in its own
# per-effort folder (mirroring purchasing-agent/) so it never collides with the
# Tool 1 dashboard's global _bmad-output/implementation-artifacts/sprint-status.yaml.
#
# -----------------------------------------------------------------------------
# BUILD GATE (read first - this is why everything is backlog)
# -----------------------------------------------------------------------------
# The heavy build is GATED behind FARMER VALIDATION (PRD Open Q4 / decision D14).
# This breakdown is HOW we build once validated, not a greenlight to start before
# a real grower confirms the data-first / gentle-surfacing thesis. No story is
# promoted to ready-for-dev until (a) the gate clears and (b) its story file is
# created via bmad-create-story. Until then every item stays backlog by design.
#
# -----------------------------------------------------------------------------
# PROPOSED SPRINT SLICING (one epic = one value increment = one feedback boundary)
# -----------------------------------------------------------------------------
# Sprint 1  Epic 7 - Almond drives the screen (the navigation operator).
#           Establishes the two foundations every later skill inherits: the
#           canonical surface registry and the read-only, farm-scoped skill factory.
#   7-1 canonical surface registry + nuqs call-site refactor (foundation, unblocks 7.3/7.4)
#   7-2 extend the tool factory into the skill framework
#   7-3 the navigate skill, entity resolver, and ambiguity rule
#   7-4 the server->client navigation bridge
#   7-5 action chips and the never-hijack guarantee
#
# Sprint 2  Epic 8 - Almond hands you a spreadsheet (the first artifact + its memory).
#           The largest epic (7 stories): the full-data law, the exporters, the
#           coverage footer, the owner gate, and the Reports persistence unit Epic 9 reuses.
#   8-1 the uncapped, farm-scoped full-data export loader
#   8-2 spreadsheet builders - CSV reuse adapter + the XLSX path
#   8-3 the bill-due-schedule exporter (SCHEDULED never shown as billed)
#   8-4 the shared coverage / as-of footer composer
#   8-5 the exportSpreadsheet skill with preview + immediate download
#   8-6 Reports persistence - GeneratedReport, private Blob, owner-scoped download route
#   8-7 the Reports area
#
# Sprint 3  Epic 9 - Almond builds the lender-ready PDF (the shareable proof).
#           Reuses Epic 8 wholesale (loader, footer, persistence). The demo's closer.
#   9-1 the tested PDF section-template library
#   9-2 the PDF composer (generative selection over the full-data path)
#   9-3 the generateReport skill with preview, save, and download
#
# Sprint 4  Epic 10 - Surfacing Almond, gently (discoverability without overbearing).
#           Surfaces whatever shipped; closes the abuse gap the generative endpoint opened.
#   10-1 action and export-flavored starters
#   10-2 rail entry and the calm first-run nudge
#   10-3 abuse and cost protection on the generative endpoint (required before wide Tour)
#
# -----------------------------------------------------------------------------
# DEPENDENCY ORDER (validated in epics.md Step 5 - backward-only, no forward deps)
# -----------------------------------------------------------------------------
# Epic independence: Epic 7 is standalone. Epic 8 builds on Epic 7's factory (7.2)
# and delivers complete spreadsheet value alone. Epic 9 builds on Epic 8's
# loader/footer/persistence and delivers the PDF alone. Epic 10 surfaces whatever
# shipped. NO epic requires a later epic.
# Within-epic flow: 7.1->7.2->7.3->7.4->7.5 ; 8.1->8.2->8.3->8.4->8.5->8.6->8.7 ;
# 9.1->9.2->9.3 ; 10.1->10.2->10.3. Each story builds only on previous stories
# (and earlier epics). 7.3 emits the NavigateAction that 7.4 consumes (backward dep).
# Deps + env land in the first story that needs each (no upfront install-everything
# story): exceljs in 8.2, @vercel/blob + BLOB_READ_WRITE_TOKEN in 8.6,
# @react-pdf/renderer in 9.1. GeneratedReport model is created in 8.6, not upfront.
#
# -----------------------------------------------------------------------------
# OPEN ITEMS (not blockers to writing stories; settle with the team)
# -----------------------------------------------------------------------------
# - Numeric activation + generation-latency targets (NFR4) are TBD.
# - The exact gentle-surfacing copy for Story 10.2 (PRD Open Q2).
# - Build-time confirmations: surface-registry refactor (7.1) lands first; confirm
#   the @vercel/blob private-read mechanism against the installed SDK (8.6);
#   rate-limiting (10.3) before wide public-Tour exposure.
# =============================================================================

# STATUS DEFINITIONS:
# ==================
# Epic Status:    backlog | in-progress | done
# Story Status:   backlog | ready-for-dev | in-progress | review | done
# Retrospective:  optional | done

generated: 2026-06-17
last_updated: 2026-06-18T00:00:00-0700
project: Terra
project_key: ALM
tracking_system: file-system
story_location: "_bmad-output/almond/4-implementation"

development_status:
  # Epic 7: Almond drives the screen (the navigation operator)
  epic-7: in-progress
  7-1-canonical-surface-registry-and-nuqs-call-site-refactor: done
  7-2-extend-the-tool-factory-into-the-skill-framework: done
  7-3-the-navigate-skill-entity-resolver-and-ambiguity-rule: backlog
  7-4-the-server-client-navigation-bridge: backlog
  7-5-action-chips-and-the-never-hijack-guarantee: backlog
  epic-7-retrospective: optional

  # Epic 8: Almond hands you a spreadsheet (the first artifact and its memory)
  epic-8: backlog
  8-1-the-uncapped-farm-scoped-full-data-export-loader: backlog
  8-2-spreadsheet-builders-csv-reuse-adapter-and-the-xlsx-path: backlog
  8-3-the-bill-due-schedule-exporter-scheduled-never-shown-as-billed: backlog
  8-4-the-shared-coverage-as-of-footer-composer: backlog
  8-5-the-exportspreadsheet-skill-with-preview-and-immediate-download: backlog
  8-6-reports-persistence-generatedreport-private-blob-and-the-owner-scoped-download-route: backlog
  8-7-the-reports-area: backlog
  epic-8-retrospective: optional

  # Epic 9: Almond builds the lender-ready PDF (the shareable proof)
  epic-9: backlog
  9-1-the-tested-pdf-section-template-library: backlog
  9-2-the-pdf-composer-generative-selection-over-the-full-data-path: backlog
  9-3-the-generatereport-skill-with-preview-save-and-download: backlog
  epic-9-retrospective: optional

  # Epic 10: Surfacing Almond, gently (discoverability without overbearing)
  epic-10: backlog
  10-1-action-and-export-flavored-starters: backlog
  10-2-rail-entry-and-the-calm-first-run-nudge: backlog
  10-3-abuse-and-cost-protection-on-the-generative-endpoint: backlog
  epic-10-retrospective: optional
