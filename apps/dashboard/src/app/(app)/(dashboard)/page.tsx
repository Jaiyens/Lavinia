import { EnergyDashboard } from "../_components/energy-dashboard";

// Home == the Energy dashboard today (it grows a cross-agent strip when a 2nd agent ships).
export default function HomePage() {
  return <EnergyDashboard />;
}
