// STUB — replaced by the Grower Details screen agent (Last/All Deliveries + Reports panel + variety
// pie). Reads the active context from searchParams (hullerId, cropYear) and the data contract
// (loadCropDeliveries, varietyWeights, REPORT_LIST).
export default function AlmondGrowerStub() {
  return (
    <div className="flex min-h-[16rem] items-center justify-center rounded-[var(--radius-lg)] border border-dashed border-outline-variant text-on-surface-variant">
      <p className="type-body-md">Grower Details — building.</p>
    </div>
  );
}
