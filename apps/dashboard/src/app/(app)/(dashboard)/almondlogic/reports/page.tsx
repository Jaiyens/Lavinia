// STUB — replaced by the Reports screen agent (the report list + data-driven report views). Reads
// the active context from searchParams (hullerId, cropYear) and the data contract (REPORT_LIST,
// loadCropDeliveries, loadRuns).
export default function AlmondReportsStub() {
  return (
    <div className="flex min-h-[16rem] items-center justify-center rounded-[var(--radius-lg)] border border-dashed border-outline-variant text-on-surface-variant">
      <p className="type-body-md">Reports — building.</p>
    </div>
  );
}
