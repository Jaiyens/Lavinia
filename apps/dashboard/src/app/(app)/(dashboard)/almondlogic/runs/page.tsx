// STUB — replaced by the Runs screen agent (the runs/turnout table). Reads the active context from
// searchParams (hullerId, cropYear) and the data contract (loadRuns).
export default function AlmondRunsStub() {
  return (
    <div className="flex min-h-[16rem] items-center justify-center rounded-[var(--radius-lg)] border border-dashed border-outline-variant text-on-surface-variant">
      <p className="type-body-md">Runs — building.</p>
    </div>
  );
}
