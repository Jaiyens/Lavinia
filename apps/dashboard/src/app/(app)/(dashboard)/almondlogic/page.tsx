// STUB — replaced by the Home screen agent (Grower Portal News + Recent Activity). The portal shell
// (layout.tsx) provides the grower header, sub-nav, and hullers/handlers sidebar; this page renders
// the Home content using src/lib/almond-portal/data.ts (loadRecentActivity).
export default function AlmondHomeStub() {
  return (
    <div className="flex min-h-[16rem] items-center justify-center rounded-[var(--radius-lg)] border border-dashed border-outline-variant text-on-surface-variant">
      <p className="type-body-md">Home — building.</p>
    </div>
  );
}
