import { EnergyDashboard } from "../../_components/energy-dashboard";

// The Energy agent: the PG&E tool. Same surface as Home today; the rail distinguishes them.
export default function EnergyPage() {
  return <EnergyDashboard />;
}
