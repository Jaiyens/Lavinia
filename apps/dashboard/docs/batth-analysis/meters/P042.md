# Meter Dossier (metadata-only) — P042 — PUMP # 42

> SA ID `6531180292` · Meter `1009487664` · Account `6539944461-4` · KANWARJIT BATTH & GAGANDIP BATTH
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 21; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P042** |
| PG&E Service Agreement (SA) ID | **6531180292** |
| Meter serial / number | **1009487664** |
| Descriptor (inventory) | `PUMP # 42` |
| Legal / billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account number | **6539944461-4** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **BIG BLOCK** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **1500 gpm** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **GOOD** |
| Latitude / Longitude | **36.514264, -119.908391** |
| Inventory row | 21 |
| Solar flag | **TRUE** |
| Solar group | `4939` |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is grower pump **P042** serving on BIG BLOCK, rated **1500 gpm**, flagged **GOOD** by the grower.

It **is** flagged into the farm's solar/NEM aggregation (see section d). **Ground-truth reminder:** the two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `AGC` → **AGC / AG-C (modern Schedule AG, ≥35 kW, high use) — secondary voltage.**
- **Plain English:** `AGC` is the **modern (current) Schedule AG, Rate AG-C** — the ≥35 kW *high-use* default. AG-C is the **demand-heavy** AG sub-rate: a **Max-Demand charge ($21.43/kW secondary)** *plus* a punishing **Summer Peak-Demand charge of $29.92/kW** on the highest 15-min demand during the **5-8pm** window in summer, plus the highest AG customer charge (~$43.58/mo). In exchange the *energy* is the cheapest of the four (peak-summer ~$0.213/kWh).
- **Demand Charge Rate Limiter:** AG-C is the only AG sub-rate with the $0.50/kWh blended cap (ex-customer-charge), which protects spiky low-energy meters.
- **The core arbitrage:** AG-C only pays off when the meter pushes serious sustained kW. A meter whose *real* max 15-min demand now sits **comfortably below 35 kW** is almost certainly cheaper on **AG-A1** (no demand charge, ~$0.69/day) or **AG-A2**. PG&E defaults meters UP to AG-C on a single 35 kW demand spike but never demotes them back automatically — so AG-C meters are the prime 'demote-to-AG-A1' sweep candidates.
- **Demand exposure:** high and structural. This is exactly the rate where a trailing-12-month interval pull most often finds money.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `AGC`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **YES (aggregation member).** `solarFlag: true`, solar group `4939` (a NEM2-aggregation legacy-TOU roster group), but `nemType` is null on this inventory row.
- This meter is tagged into aggregation group `4939`. **Ground truth:** the two real arrays total **1,932 kW (840 + 1,092)**; group labels like `4939` are roster tags, NOT additional arrays and NOT 12,180 kW.
- **True-up month:** **not recorded on this row** (`trueUpMonth: null`). The arrangement still settles annually; the true-up date lives on the generating/account record. Pull the arrangement's anniversary date to confirm.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `6539944461-4`** (KANWARJIT BATTH & GAGANDIP BATTH). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `6531180292` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.
   - **Priority note:** `AGC` is a **demand-heavy / high-use** schedule. If this meter's true max 15-min demand is **comfortably below 35 kW** (especially in the 5-8pm window), it is a prime **demote-to-AG-A1/AG-A2** candidate worth (typically) hundreds to low-thousands $/yr. But that is **only** confirmable with the interval pull — do not assert a number from metadata.
3. **The Generation Allocation Summary + Form 79-1202 roster** for this aggregation arrangement (solar group `4939`). For any solar/NEM meter this is what proves the allocated-credit share and confirms the meter isn't orphaned (the P031 zero-credit failure mode). For an `NEMEXP`/`NEMEXPM`-tagged SA it is essential.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

P042 (BIG BLOCK) is on **AGC** (AGC / AG-C) and is in the farm's solar/NEM aggregation, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `6539944461-4` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

