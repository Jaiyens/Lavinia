# P026 (PUMP # 26) — CHATEAU FRESNO — metadata-only dossier

> Source: `normalized/by-meter/3372050998.json` (primary), `normalized/inventory.json` row 50, `normalized/manifest.json` idx 49.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P026** |
| Descriptor | PUMP # 26 |
| PG&E Service Agreement (SA) ID | **3372050998** |
| Meter serial # | **1010785905** |
| Billing entity | **BATTH,CHARANJIT S** |
| PG&E account # | **3372050929-9** |
| Ranch | CHATEAU FRESNO |
| Crop served | ALMONDS |
| Irrigation source | RCWD |
| Pump capacity (GPM) | **2,600 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | NEW WELL |
| Installed / drilled | 2013 |
| Latitude / Longitude | **36.521183, -119.940634** |
| Inventory row | 50 |

This SA is a 2,600 GPM pump, serving almonds, on the CHATEAU FRESNO ranch, with RCWD as the surface-water source. Billing entity **BATTH,CHARANJIT S** on account **3372050929-9**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `AGB`.** Family: **AG-B** (current).

- **Plain English.** Current AG-B (large, 35+ kW). AG-B has NO separately-stated demand charge: its cost is a customer charge plus time-of-use energy only. Best for pumps whose peak kW is high relative to their kWh (short, intense runs) where a demand charge would sting.
- **Nearest alternative schedules.** AG-C2 (the demand-charge alternative within the large family)
- **What the rate lever does here.** Current schedule, so the lever stays SILENT without a back-test (off-band current meters never get a qualitative finding, unlike legacy ones). The AG-B vs AG-C2 choice turns entirely on the load factor, which only interval kWh can reveal.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** Solar group label **Solar** (a grouping/array tag, not a per-meter nameplate). Per-meter kW is null on this row; true array size lives on the farm-level solar inventory (the two real arrays total 1,932 kW).
- **NEM arrangement:** **enrolled** — NEM Paired-storage / multi (NEMEXPM): an exporting NEM arrangement with a paired/multi-meter configuration.
- **True-up month:** **January** (annual NEM settlement month).

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 3372050929-9** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **3372050998**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a within-family switch dollar. Until then it is silent.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement for January). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "P026 (almonds, CHATEAU FRESNO) runs on **AGB** under account 3372050929-9, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 3372050929-9 bill PDF or a UtilityAPI feed for SA 3372050998, plus the solar Generation Allocation Summary, and the levers can run."
