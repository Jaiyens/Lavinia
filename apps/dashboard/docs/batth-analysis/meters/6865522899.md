# Meter Dossier (metadata-only) — SA 6865522899 — metadata-only meter

> SA ID `6865522899` · Meter `1010127901` · Account `6863917471-0` · K S BATTH & G S BATTH PARTNERSHIP
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 34; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **none (non-pump SA — no grower pump number)** |
| PG&E Service Agreement (SA) ID | **6865522899** |
| Meter serial / number | **1010127901** |
| Descriptor (inventory) | none recorded |
| Legal / billing entity | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account number | **6863917471-0** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **not recorded** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **not in records** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **not recorded** |
| Latitude / Longitude | **36.532414, -119.888271** |
| Inventory row | 34 |
| Solar flag | **TRUE** |
| Solar group | `5219` |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | `NEM2AG` |
| True-up month | May |

This is a **non-pump / metadata-only service agreement** (no grower pump number recorded).

It **is** flagged into the farm's solar/NEM aggregation (see section d). **Ground-truth reminder:** the two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `A1X` → **A1X / AG-1 (legacy small flat ag, no TOU).**
- **Plain English:** `A1X` is the billing token for **legacy Schedule AG-1**, the old *small* agricultural rate. AG-1 is **non-TOU (flat energy)** with a tiny daily customer charge (~$0.574/day, Rate A) and a connected-load demand charge on the larger Rate B (~$21.39/$17.26 per kW summer/winter). It is a **closed legacy schedule** (closed to new enrollment since 3/1/2020).
- **Why it survives here:** legacy AG-1 is kept for solar-legacy customers (to hold the legacy NEM/TOU window) or meters still without a remotely-read interval meter. On this farm the `A1X` meters are paired to a NEM2-aggregation **generating** service (see NEM block) and parked here to preserve the legacy treatment until solar-legacy eligibility lapses (no later than **2027**).
- **On lapse:** when legacy eligibility ends, AG-1 force-transitions to the modern Schedule AG family (size-class dependent: <35 kW → AG-A1/AG-A2, ≥35 kW → AG-B/AG-C).
- **Demand exposure:** flat energy, no 5-8pm peak-demand charge. Low structural risk; the value here is the **NEM aggregation generating role**, not the rate itself.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `A1X`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **YES — `NEM2AG` = NEM2 Aggregation *GENERATING* meter.** This is (or is tied to) the **one generating account** physically connected to the array for its aggregation arrangement. There can be only one generating meter per arrangement.
- **This SA is where the exports and the allocation originate.** Its **Generation Allocation Summary** is the document that proves which benefiting SAs got what share. The generating account is permanently ineligible for Net Surplus Compensation under NEMA.
- **Solar group label:** `5219` (aggregation roster tag). **Reminder of ground truth:** real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW.
- **True-up month:** **May.** The 12-month NEM2 settlement for this aggregation arrangement reconciles in **May**; the carried-forward dollar balance (charges minus allocated credits) is collected then. To audit it you need the **May True-Up Statement + the Generation Allocation Summary** for this SA.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `6863917471-0`** (K S BATTH & G S BATTH PARTNERSHIP). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `6865522899` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.
3. **The Generation Allocation Summary + Form 79-1202 roster** for this aggregation arrangement (solar group `5219`). For any solar/NEM meter this is what proves the allocated-credit share and confirms the meter isn't orphaned (the P031 zero-credit failure mode). For an `NEMEXP`/`NEMEXPM`-tagged SA it is essential.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

SA 6865522899 is on **A1X** (A1X / AG-1) and is in the farm's solar/NEM aggregation, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `6863917471-0` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

