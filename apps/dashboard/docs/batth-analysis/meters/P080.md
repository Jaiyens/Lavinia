# P080 (PUMP 80) — SWANSON — metadata-only dossier

> Source: `normalized/by-meter/6538183119.json` (primary), `normalized/inventory.json` row 24, `normalized/manifest.json` idx 23.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P080** |
| Descriptor | PUMP 80 |
| PG&E Service Agreement (SA) ID | **6538183119** |
| Meter serial # | **1010047087** |
| Billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account # | **6539944461-4** |
| Ranch | SWANSON |
| Crop served | not recorded |
| Irrigation source | not recorded |
| Pump capacity (GPM) | **2,500 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | NEW WELL |
| Installed / drilled | 2013 |
| Latitude / Longitude | **36.488917, -119.874278** |
| Inventory row | 24 |

This SA is a 2,500 GPM pump, on the SWANSON ranch. Billing entity **KANWARJIT BATTH & GAGANDIP BATTH** on account **6539944461-4**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `AGC`.** Family: **AG-C** (current).

- **Plain English.** Current AG-C (large, 35+ kW). AG-C is the demand-charge large ag schedule: customer charge + TOU energy + a summer peak-period demand charge ($/kW). Best for pumps that run long and steady (high load factor) where lower energy rates beat the demand adder.
- **Nearest alternative schedules.** AG-B2 (the no-demand-charge alternative within the large family)
- **What the rate lever does here.** Current schedule, so the lever stays SILENT without a back-test. The AG-C vs AG-B choice is load-factor-driven and needs interval kWh. The bare AG-C->AG-B 'savings' the engine can emit from summaries alone are sign-ambiguous artifacts and must not be quoted.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** Solar group label **4939** (a grouping/array tag, not a per-meter nameplate). Per-meter kW is null on this row; true array size lives on the farm-level solar inventory (the two real arrays total 1,932 kW).
- **NEM arrangement:** Tagged solar (solarFlag=true) but no NEM type is recorded on the row. The meter sits inside a solar group (label 4939) but its NEM arrangement is not captured in the inventory.
- **True-up month:** not recorded on this row.

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 6539944461-4** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **6538183119**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a within-family switch dollar. Until then it is silent.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "P080 (SWANSON) runs on **AGC** under account 6539944461-4, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 6539944461-4 bill PDF or a UtilityAPI feed for SA 6538183119, plus the solar Generation Allocation Summary, and the levers can run."
