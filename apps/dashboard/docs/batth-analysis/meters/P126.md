# Meter Dossier (metadata-only) — P126 — EAST RANCH N

> SA ID `1907047703` · Meter `1010782395` · Account `1909940814-8` · K S BATTH & G S BATTH PARTNERSHIP
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 111; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P126** |
| PG&E Service Agreement (SA) ID | **1907047703** |
| Meter serial / number | **1010782395** |
| Descriptor (inventory) | none recorded |
| Legal / billing entity | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account number | **1909940814-8** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **EAST RANCH N** |
| Crop | **ALMONDS** |
| Irrigation type | **CONSOLIDATED** |
| Flow (GPM) | **500 gpm** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **GOOD** |
| Latitude / Longitude | **36.602996, -119.771551** |
| Inventory row | 111 |
| Solar flag | **FALSE** |
| Solar group | none |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is grower pump **P126** serving almonds on EAST RANCH N, rated **500 gpm**, flagged **GOOD** by the grower.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `HAGA2` → **HAGA2 / AG-A2 at transmission (high) voltage (<35 kW, high use).**
- **Plain English:** `HAGA2` is **modern Schedule AG Rate AG-A2 at transmission voltage**. AG-A2 is the **<35 kW high-use** rate: a flat ~$0.69/day customer charge, a **Max-Demand charge (~$11.79/kW secondary; transmission ~$4.68/kW) but NO 5-8pm peak-demand charge**, and energy cheaper than AG-A1.
- **Sits between AG-A1 and AG-C:** a modest demand charge buys materially lower energy than AG-A1. Correct for a small meter that runs *many hours* (high load factor) but stays under 35 kW.
- **Demand exposure:** modest (max-demand only, no peak-demand). Already a low-risk small-tier rate; the only check is AG-A1 vs AG-A2 (a few hundred dollars), which needs interval kWh to call.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `HAGA2`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`. No solar on this meter, no aggregation share, no import/export, **no annual true-up.**
- This is a plain full-retail load meter. Nothing to allocate, nothing to settle up.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `1909940814-8`** (K S BATTH & G S BATTH PARTNERSHIP). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `1907047703` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

P126 (almonds, EAST RANCH N) is on **HAGA2** (HAGA2 / AG-A2 at transmission) with no solar, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `1909940814-8` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

