# Meter Dossier (metadata-only) — P114 — AIRPORT

> SA ID `8923940171` · Meter `1010117036` · Account `8922820273-8` · KANWARJIT BATTH & GAGANDIP BATTH
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 107; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P114** |
| PG&E Service Agreement (SA) ID | **8923940171** |
| Meter serial / number | **1010117036** |
| Descriptor (inventory) | none recorded |
| Legal / billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account number | **8922820273-8** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **AIRPORT** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **800 gpm** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **BAD** |
| Latitude / Longitude | **36.532551, -119.91273** |
| Inventory row | 107 |
| Solar flag | **FALSE** |
| Solar group | none |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is grower pump **P114** serving on AIRPORT, rated **800 gpm**, flagged **BAD** by the grower.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `HAGFB` → **HAGFB / AG-F (flexible off-peak) B-tier at transmission voltage.**
- **Plain English:** `HAGFB` is the billing code for PG&E's **optional Schedule AG-F (flexible/relaxed off-peak)**, B-tier (≥35 kW medium use), taken at transmission voltage. AG-F is an elective variant within the AG family that shifts the off-peak window; a customer 'may elect any rate for which they are eligible,' including AG-F.
- **Why it appears:** AG-F is chosen when the operator can shift load into the flexible off-peak to dodge peak energy/demand. Whether it beats plain AG-B/AG-C for *this* meter depends entirely on the load shape.
- **Demand exposure:** depends on the elected AG-F structure; treat like the B-tier (no/low demand charge) but confirm against the live AG-F sheet. **Caveat:** AG-F economics are *purely* a load-shape question — undecidable without interval kWh.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `HAGFB`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`. No solar on this meter, no aggregation share, no import/export, **no annual true-up.**
- This is a plain full-retail load meter. Nothing to allocate, nothing to settle up.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `8922820273-8`** (KANWARJIT BATTH & GAGANDIP BATTH). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `8923940171` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.
   - **Priority note:** `HAGFB` is a **demand-heavy / high-use** schedule. If this meter's true max 15-min demand is **comfortably below 35 kW** (especially in the 5-8pm window), it is a prime **demote-to-AG-A1/AG-A2** candidate worth (typically) hundreds to low-thousands $/yr. But that is **only** confirmable with the interval pull — do not assert a number from metadata.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

P114 (AIRPORT) is on **HAGFB** (HAGFB / AG-F) with no solar, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `8922820273-8` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

