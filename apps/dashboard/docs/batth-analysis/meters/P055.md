# P055 — Pump # 55, Swanson (SA 4699664820)

> Per-meter dossier. Source of truth: `normalized/by-meter/4699664820.json` (manifest idx 86, `billed: true`).
> All savings dollars below are computed by DETERMINISTIC pure functions in `src/lib/energy` — no AI. The only AI in the pipeline is bill-PDF vision extraction in `src/lib/extract`.

---

## (a) IDENTITY

| Field | Value | Source |
|---|---|---|
| Grower pump id | **P055** | inventory `growerPumpId` |
| PG&E SA ID (service id) | **4699664820** | inventory `serviceId` |
| Meter serial / number | **1010131530** | inventory `meterSerial` = billing `meterNumber` |
| Bill descriptor | **PUMP # 55** | inventory `descriptor` / billing `saIdDescriptor` |
| Legal billing entity | **BATTH, CHARANJIT S** | inventory `billingName` |
| Account number | **4699664587-8** (the ~46-SA master bill account) | inventory `account` |
| Ranch | **SWANSON** | inventory `ranch` |
| Crop | not on file (`crop: null`) | inventory |
| Flow (GPM) | **250 GPM** | inventory `gpm` |
| Horsepower (HP) | **not on file** | inventory |
| Status | **GOOD** | inventory `status` |
| Latitude / Longitude | **36.503649, -119.865734** (has coordinates) | inventory |
| Row in master inventory | 87 | inventory `rowNumber` |

Note: `billing.growerPumpId` is null but the inventory ties serial 1010131530 → P055, and both name "PUMP # 55" / "PUMP #55". Same physical meter. Filed under P055.

---

## (b) RATE SCHEDULE — decoded in plain English

**On the bill: `HAGC` (inventory `rateSchedule`), which prints as "AGC Ag35+ kW High Use" — i.e. PG&E Schedule AG-C, large / `35+ kW` tier.**

What AG-C is, in operator English: AG-C is a **current** (not legacy) agricultural time-of-use power rate for larger pumps (the "35+ kW" / High Use family). It carries three pieces: a per-day **customer charge**, per-kWh **TOU energy** split by time-of-use window, and a per-kW **max-demand** charge (plus, in summer only, a peak-period demand component capped by a demand-charge limiter).

Decoded values for the AG-C / `large` (AG-C2) row of the rate card (`fixtures/pge-ag-rate-card.json`, version 2026-06.1, effective 2026-03-01):
- **Customer charge:** **$1.43343 per day** (= $43.60/month). Matches the bill's printed $1.43343/day exactly.
- **Max-demand charge:** **$24.95/kW** (winter and summer).
- **Peak-period demand (summer only):** $9/kW, capped by a **demand-charge limiter of $0.50/kWh** (representative placeholder for summer; not billed this winter cycle).
- **TOU energy windows** (AG RATE peak = **5–8pm year-round daily**, NOT the 4–9pm DR event window): winter peak **$0.1843**, partial-peak $0.17, off-peak **$0.15861** /kWh; summer peak $0.22, partial $0.20, off-peak $0.18 /kWh (summer energy representative).

**Why it is on this rate:** It is a **current** large-pump schedule (`legacy: null/false`; the "Ag35+ kW High Use" descriptor marks it as the large/35+ kW eligibility tier). A 250 GPM Swanson pump that draws above ~35 kW when running belongs on a 35+ kW family. AG-C is the lowest-energy of the current large families (its off-peak winter energy $0.15861 undercuts AG-B's $0.30671), so this is **not** an obviously-mis-rated meter the way the legacy AG-5 meters are — it is already on a sensible current rate.

---

## (c) THE LATEST BILL — fully itemized

**Cycle:** 2026-02-11 → 2026-03-12 (30 days). Tariff: "AGC Ag35+ kW High Use". **Printed total: $45.89.** Total energy: **14.006 kWh**. Billed max demand: **0.024 kW**. `coverageState: "reconciled"`. This is a low-use winter month — the pump barely ran — straddling the 2026-03-01 rate change, so each charge type splits into a pre-change and a post-change sub-period.

| Line | Window | Qty | Rate | Amount |
|---|---|---|---|---|
| Customer Charge 02/11–02/28 | 18 days | — | $1.43343/day | **$25.80** |
| Customer Charge 03/01–03/12 | 12 days | — | $1.43343/day | **$17.20** |
| TOU energy — **Peak** (pre-change) | 5–8pm | 1.121 kWh | $0.18430/kWh | **$0.21** |
| TOU energy — **Off-Peak** (pre-change) | | 7.304 kWh | $0.15861/kWh | **$1.16** |
| TOU energy — **Peak** (post-change) | 5–8pm | 0.746 kWh | $0.18550/kWh | **$0.14** |
| TOU energy — **Off-Peak** (post-change) | | 4.835 kWh | $0.15981/kWh | **$0.77** |
| **Demand charge** (max demand) | — | 0.024 kW | (per-kW) | **$0.61** |
| Non-bypassable charges (NBC) | — | — | — | $0.00 |
| Energy Commission Tax | — | — | — | $0.00 |
| NEM net charge | — | — | — | $0.00 (no solar) |
| **PRINTED TOTAL** | 30 days | 14.006 kWh | | **$45.89** |

**Reconciliation arithmetic (deterministic):**
- Customer: 18 × $1.43343 = $25.80174 → **$25.80**; 12 × $1.43343 = $17.20116 → **$17.20**; subtotal **$43.00**.
- Energy: $0.21 + $1.16 + $0.14 + $0.77 = **$2.28** (matches the printed TOU rollup: Peak 1.867 kWh = $0.35, Off-Peak 12.139 kWh = $1.93; 1.867 + 12.139 = **14.006 kWh** = `totalKwh`).
- Demand: **$0.61** on a 0.024 kW max demand.
- **$43.00 + $2.28 + $0.61 = $45.89.** Matches the printed total exactly.

**Peak / off-peak split:** Peak total 1.867 kWh → $0.35; Off-Peak total 12.139 kWh → $1.93. No partial-peak energy this cycle. The demand line is trivially small ($0.61) because the pump barely ran in February.

---

## (d) SOLAR / NEM status + true-up

**No solar. No NEM.** `solarFlag: false`, `nemType: null`, `solarKw: null`, `trueUpMonth: null`, and `billing.nem: null`. There is no import/export trail and no true-up for this meter. It is a straight grid-served pump.

---

## (e) SAVINGS FINDINGS — every applicable finding, with arithmetic, engine, and data have/need

### Finding 1 — Rate optimization (AG-C → AG-B): SUPPRESSED as a sign-ambiguous artifact, do NOT quote a dollar
- **Engine:** `rateLever()` in `src/lib/energy/rate-lever.ts`, via `runRateLever()`. **Deterministic pure function** (no AI).
- **What the engine would do:** This meter is **not solar**, so the runner DOES pass its reconciled periods to the lever. The schedule maps to AG-C / large (`realTier: "large"`). For a large meter the candidate set is `["AG-B2", "AG-C2"]` minus the current plan, so the only counterfactual is **AG-C → AG-B2**. The lever back-tests the current AG-C charges against the card (`backTestMeter`) within the `BACK_TEST_BAND_PCT = 5%` band, then prices the same cycles under AG-B2.
- **Why any emitted dollar here is an ARTIFACT, not a real saving (GROUND TRUTH):** The only billed cycle is a **single 14.006 kWh winter month** with a 0.024 kW demand. There is **no 15-minute interval kWh** on file — the bill summary carries only this one trivial cycle. The non-legacy usage-basis guard in `rateLever` (`totalKwhTested <= 0` → `no_usage_basis`) is technically cleared (14 kWh > 0), but 14 kWh is far too thin to trust: the entire delta between AG-C and AG-B on this cycle is driven by the **fixed customer-charge gap** ($1.43343/day AG-C vs $0.91565/day AG-B = $0.51778/day × 30 = ~$15.53) and a tiny energy delta, NOT by real operating behavior. Per the binding ground truth, **the AG-C→AG-B "savings" the engine emits without intervals are sign-ambiguous artifacts** — winter under-states summer demand, and AG-B carries no peak-period demand limiter, so a summer cycle could flip the sign entirely. **Do not surface this as a banked saving.** Treat it as "needs interval data to confirm."
- **Data we HAVE:** the AG-C schedule label, one reconciled 14.006 kWh winter cycle (fully reconciled to $45.89), the customer-charge and TOU-energy line items, the 0.024 kW demand.
- **Data we NEED to make the AG-C→AG-B comparison trustworthy:** (1) **15-minute interval kWh for a full year**, including a summer cycle that bills a real peak-window demand (the card cannot honestly price an AG-C summer peak-period demand candidate without a parsed peak-window kW — see `peakWindowDemandKw: null` in `rate-lever.ts`), and (2) several more same-season cycles so the back-test band reflects real usage rather than one near-idle month.

### Finding 2 — Bill audit (anomalous cycle): NONE
- **Engine:** `billAudit()` in `src/lib/energy/bill-audit.ts` (deterministic; compares the meter's own bills to each other).
- **Result:** **No finding.** Only **one** cycle on file (`cycleCount: 1`); `billAudit` needs `minComparators` (default 3) same-season comparable cycles for a stable median. With a single cycle the audit cannot run and emits nothing.
- **Need:** at least 3 more same-season billed cycles.

### Finding 3 — Solar / NEM demand insight: NOT APPLICABLE
- **Engine:** `nemDemandInsight()` / `solarNemChecks()` in `src/lib/energy/solar-nem.ts`.
- **Result:** **No finding.** Although this meter IS on the AG-C demand family and DOES owe a (tiny $0.61) demand charge, the solar engines require `isSolar` / `solarKw !== null`. This meter has **no solar**, so both `nemDemandInsight` (returns null on `!isSolar`) and `solarNemChecks` (returns `[]` on `solarKw === null`) short-circuit immediately. Correct: there is no solar story to tell here.

### Finding 4 — Demand charge exposure: negligible
- The $0.61 demand charge on 0.024 kW is far below any threshold worth flagging. No demand-charge finding.

**Notable-finding flag (>$500/yr): NO.** No trustworthy deterministic savings finding exists for this meter on the data we have. The only candidate (AG-C→AG-B) is a sign-ambiguous, interval-dependent artifact on a single 14 kWh winter cycle and must not be quoted as a dollar saving.

---

## (f) One-line plain-operator-English summary

**Pump #55 (Swanson) is a small grid-only pump already on a sensible current rate (AG-C); its February bill was just $45.89 and reconciles to the penny — there is no solar here and no real rate-savings dollar to claim until a full year of 15-minute meter data loads.**
