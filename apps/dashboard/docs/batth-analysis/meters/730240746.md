# SA 730240746 (DEHYDRATOR) — metadata-only dossier

> Source: `normalized/by-meter/730240746.json` (primary), `normalized/inventory.json` row 20, `normalized/manifest.json` idx 19.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **none (non-pump or unlabeled SA)** |
| Descriptor | DEHYDRATOR |
| PG&E Service Agreement (SA) ID | **730240746** |
| Meter serial # | **1010898065** |
| Billing entity | **BATTH,CHARANJIT S** |
| PG&E account # | **0730240888-0** |
| Ranch | not recorded |
| Crop served | not recorded |
| Irrigation source | not recorded |
| Pump capacity (GPM) | not recorded |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | not recorded |
| Installed / drilled | not recorded |
| Latitude / Longitude | **36.561665, -119.873839** |
| Inventory row | 20 |

This SA has **no pump id, crop, ranch, or GPM** recorded — it is most likely a non-pump or service/meter-only agreement, identifiable only by its SA ID, serial, account, and coordinates. Billing entity **BATTH,CHARANJIT S** on account **0730240888-0**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `B1`.** Family: **B-1 (non-ag small general)** (current).

- **Plain English.** B-1 is PG&E's small general-service (non-agricultural) rate, used here for non-pump loads (e.g. a dehydrator or an office). It is a flat/tiered small-commercial rate, not a TOU ag pump schedule.
- **Nearest alternative schedules.** Not an ag-rate candidate. (If genuinely a process load, B-6/B-10 TOU options exist, but that is a non-ag review.)
- **What the rate lever does here.** NON-AG schedule: mapScheduleLabel returns null for B1 (the engine notes 'non-ag labels (B1...) return null'). The ag rate lever never touches this meter. The 4 non-ag B1 SAs were explicitly excluded from the lever's back-test calibration.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** Solar group label **4624** (a grouping/array tag, not a per-meter nameplate). Per-meter kW is null on this row; true array size lives on the farm-level solar inventory (the two real arrays total 1,932 kW).
- **NEM arrangement:** **enrolled** — NEM Paired-storage / multi (NEMEXPM): an exporting NEM arrangement with a paired/multi-meter configuration.
- **True-up month:** **May** (annual NEM settlement month).

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 0730240888-0** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **730240746**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **The automated ag rate lever does NOT apply.** `mapScheduleLabel` returns null for this non-ag schedule, so no automated rate dollar is ever built on it. With the bill in hand a **human tariff review** could judge whether the load is mis-classified (an ag pump sitting on a non-ag rate), but that is a manual call, not the engine's.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement for May). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "SA 730240746 runs on **B1** under account 0730240888-0, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 0730240888-0 bill PDF or a UtilityAPI feed for SA 730240746 (it is a non-ag B-1 (non-ag small general) meter — the automated ag lever never touches it; any reclassification is a manual tariff review), plus the solar Generation Allocation Summary, and the levers can run."
