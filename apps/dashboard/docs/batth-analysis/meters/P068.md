# P068 (PUMP 68) — HOME RANCH — metadata-only dossier

> Source: `normalized/by-meter/8764708693.json` (primary), `normalized/inventory.json` row 68, `normalized/manifest.json` idx 67.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P068** |
| Descriptor | PUMP 68 |
| PG&E Service Agreement (SA) ID | **8764708693** |
| Meter serial # | **1010127892** |
| Billing entity | **BATTH,CHARANJIT S** |
| PG&E account # | **8760985871-4** |
| Ranch | HOME RANCH |
| Crop served | not recorded |
| Irrigation source | not recorded |
| Pump capacity (GPM) | **1,200 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | GOOD |
| Installed / drilled | not recorded |
| Latitude / Longitude | **36.532491, -119.899384** |
| Inventory row | 68 |

This SA is a 1,200 GPM pump, on the HOME RANCH ranch. Billing entity **BATTH,CHARANJIT S** on account **8760985871-4**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `AGC`.** Family: **AG-C** (current).

- **Plain English.** Current AG-C (large, 35+ kW). AG-C is the demand-charge large ag schedule: customer charge + TOU energy + a summer peak-period demand charge ($/kW). Best for pumps that run long and steady (high load factor) where lower energy rates beat the demand adder.
- **Nearest alternative schedules.** AG-B2 (the no-demand-charge alternative within the large family)
- **What the rate lever does here.** Current schedule, so the lever stays SILENT without a back-test. The AG-C vs AG-B choice is load-factor-driven and needs interval kWh. The bare AG-C->AG-B 'savings' the engine can emit from summaries alone are sign-ambiguous artifacts and must not be quoted.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** no (solarFlag=false).
- **Array / group:** No solar group / no per-meter solar kW recorded.
- **NEM arrangement:** No solar flag and no NEM type recorded. Treat as a non-solar load until a bill or generation summary shows otherwise.
- **True-up month:** not recorded on this row.
- **Grower solar note:** "research".

This meter shows **no solar/NEM tag**, so it is treated as a straight load. If a bill later shows generation, this row would need updating.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 8760985871-4** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **8764708693**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a within-family switch dollar. Until then it is silent.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **Bill audit needs >=3 same-season billed cycles** before it can flag an anomalous month; one cycle is never enough.

## (e) One-line summary

> "P068 (HOME RANCH) runs on **AGC** under account 8760985871-4, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 8760985871-4 bill PDF or a UtilityAPI feed for SA 8764708693 and the levers can run."
