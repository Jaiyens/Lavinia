# 730240595 -- metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 167, `normalized/manifest.json` (serviceId 730240595).
> **Billed = false.** This SA is **not** on billing account 4699664587-8, so **no bill PDF was extracted for it.** Everything below is from the master inventory metadata; no dollar figures are computed because there is no billed usage in hand. This is a lighter, metadata-only dossier.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **Not recorded** |
| Descriptor | Not recorded |
| PG&E Service Agreement (SA) ID | **730240595** |
| Meter serial # | **1007066742** |
| Billing entity | **BATTH,CHARANJIT S** |
| PG&E account # | **0730240888-0** |
| Ranch | Not recorded |
| Crop served | Not recorded |
| Irrigation source | Not recorded |
| Pump capacity | **Not recorded (blank GPM -- consistent with a non-pump / metadata-only service)** |
| Installed / drilled | Not recorded |
| Status (grower's own flag) | Not recorded |
| Latitude / Longitude | **36.561665, -119.873839** |
| Legacy flag | Not recorded |

This row has **no grower pump id** (`growerPumpId = null`) and no GPM. Combined with the B-1 business rate, this is most likely a **non-pump / service meter** (office, dehydrator, standby, or a misc service) rather than an irrigation pump, or a pump the grower never numbered.

---

## (b) RATE SCHEDULE -- decoded

**Stored schedule: `HB1` -- HB1 Schedule B-1 Small General Service at transmission voltage.**

- **HB1** is PG&E's billing code for **Schedule B-1 (Small General Service) taken at transmission (high) voltage**. B-1 is a **business / general-service** rate, NOT agricultural -- it appears on non-pumping loads (shops, offices, dehydrators, cold storage).
- **Charge structure:** **No demand charge** on standard B-1. Customer charge $0.32854/day single-phase or $0.82136/day poly-phase. TOU energy (peak-summer $0.471/kWh). Eligibility cap: not eligible if max demand >=75 kW for 3 consecutive months (bumps up to B-10/B-19).
- **Reading it:** a B-1 meter on this farm is almost certainly a non-irrigation service. If it actually serves a pump it may be mis-classified (>=70% ag use is required to be on an AG schedule), which would itself be a rate-review flag.

---

## (c) SOLAR / NEM status + true-up

- **Not solar-paired.** `solarFlag = false`, no NEM enrollment, no solar group, no true-up month.
- This meter draws straight grid power with no net-metering offset, so there is no annual true-up statement to reconcile. Its savings story is **pure rate optimization and demand management**, not solar.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** All savings dollars in the product are computed by **deterministic pure functions in `src/lib/energy`** (no AI); the only AI is the bill-PDF **vision extraction** in `src/lib/extract`. With only inventory metadata, every lever is blocked at the source. To turn this metadata row into defensible dollars we need, in priority order:

1. **The account bill PDF for 0730240888-0** (entity BATTH,CHARANJIT S). The vision extractor reads the printed tariff, the billed kWh, the demand (kW), the customer/demand/energy line items. This is the single highest-value pull -- it lights up the bill-audit lever and confirms the rate code.
2. **A UtilityAPI pull on this SA (730240595)** to retrieve **15-minute interval kWh**. Per the standing caveat, **rate optimization is only trustworthy with interval data** -- bill summaries carry no interval kWh, and any rate-switch "savings" the engine emits without intervals are **sign-ambiguous artifacts that must not be quoted**. Intervals give the true max 15-min demand, which is the entire AG-C/AG-A break-even test (is max demand comfortably below 35 kW across the trailing 12 months?).
3. (No solar on this meter, so no allocation summary or true-up statement applies.)

**Specific levers, and what each needs here:**

- **Rate optimization (lever 1).** NEED: the bill PDF + interval kWh to confirm the rate fits the usage.
- **Bill audit (lever, own-bill anomaly).** Needs >=3 comparable same-season billed cycles (totals + peaks). We have **zero** billed cycles for this SA. NEED: the account bill PDFs.
- **Demand-response enrollment (lever 2).** Needs the demand (kW) profile to size a PDP/CBP/BIP credit. NEED: intervals.
- **Solar / NEM (lever 4).** Not applicable -- this meter is not solar-paired.

---

## (e) One-line plain-operator-English summary

> "Service 730240595 is on account 0730240888-0, which we don't have a bill for yet, so all we know is it's on HB1 -- to find any dollars on it we need PG&E's bill PDF for that account plus a meter pull for the 15-minute usage."
