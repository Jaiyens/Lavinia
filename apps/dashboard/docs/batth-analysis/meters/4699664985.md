# Meter Dossier: SA 4699664985 — BATH FARMS OFFICE (KAMM T-56), Schedule B-1

> Slice: billed meters, position-among-billed **40** (idx 160). Second of two meters in this slice (the other is P058 / SA 4698074516).
> **This is NOT a pump.** It is the farm OFFICE service meter on a non-agricultural commercial rate (B-1). Filed under its service ID because it has no grower pump id.

---

## (a) IDENTITY

| Field | Value | Source |
|---|---|---|
| PG&E SA ID (service ID) | **4699664985** | manifest + inventory + billing |
| Meter serial # | **1010467455** | inventory.meterSerial / billing.meterNumber |
| Grower pump id | **(none — not a pump)** | inventory.growerPumpId = null |
| Descriptor | **"BATH FARMS- OFFICE KAMM T-56"** | inventory.descriptor / billing.saIdDescriptor |
| Billing entity | **BATTH, CHARANJIT S** | inventory.billingName |
| Actual owner | (not recorded) | inventory.actualOwner = null |
| PG&E account # | **4699664587-8** (the ~46-SA master account) | inventory.account |
| Ranch | (none recorded) | inventory.ranch = null |
| Crop | (n/a — office service) | inventory.crop = null |
| GPM | **(none — not a pump)** | inventory.gpm = null |
| HP | (n/a) | inventory.* none |
| Status | (none recorded) | inventory.status = null |
| Latitude / Longitude | **36.532165, -119.9556** (has coordinates) | inventory.latitude/longitude |
| Solar flag | **FALSE** | inventory.solarFlag |
| NEM type | (none) | inventory.nemType = null |
| True-up month | (none) | inventory.trueUpMonth = null |

Identity note: This SA is the **Batth Farms office / shop service at the KAMM site (transformer/pole "T-56")**, not an irrigation pump. The descriptor, the absence of a pump id, the absence of GPM/crop/status, and the small commercial usage signature all confirm it is a *building* load (lights, office equipment, shop) rather than a pump. It rides on the same Charanjit S. Batth master account (4699664587-8) as the irrigation meters but is on a completely different, **non-agricultural** rate (see b). Treat it as legible "what the office costs," not as a savings-bearing pump.

---

## (b) RATE SCHEDULE decoded in plain English

- **Inventory rate code:** `HB1` (the "H" is PG&E's internal handle; the schedule is **B-1**). The bill prints / billing engine's `latestTariff` is **"B1 Bus Low Use"**. `isLegacyTou` is flagged `true` in the billing block (this is PG&E's older small-commercial time-of-use class).
- **What B-1 is, in operator English:** B-1 is PG&E's **small commercial / "Business Low Use"** general-service rate — the rate a small *building* (an office, a shop) takes, NOT an agricultural rate and NOT a pump rate. It has a modest **daily customer charge**, per-kWh **energy priced by time-of-use** (here three buckets: Peak, Off-Peak, Super Off-Peak), and **no demand charge** at this usage level (`demandCharges: []`, `demandChargeUsd: null`). Because it is small commercial, it also carries the small state **Energy Commission Tax** line that ag schedules on this account do not show.
- **Customer charge:** the bill prints **$0.32854 per day**. The two dated sub-periods are **18 days × $0.32854 = $5.91** and **12 days × $0.32854 = $3.94**, summing to the printed customer subtotal **$9.85**. (This $/day is much lower than any ag schedule's customer charge, consistent with a small-commercial building service.)
- **Demand $/kW:** **none.** B-1 at this usage carries no demand charge — there is no $/kW line, no peak kW (`peakKw: null`), and `demandChargeUsd_structured: null`. This is the key structural difference from the ag pump meters.
- **TOU windows / energy prices (printed on this bill):**
  - **Peak — $0.40252/kWh** (highest)
  - **Off-Peak — $0.38640/kWh**
  - **Super Off-Peak — $0.36291/kWh** (lowest)
  Note these B-1 energy prices are **NOT on the ag rate card** (`fixtures/pge-ag-rate-card.json` is agricultural only). They come straight off the printed bill. "Super Off-Peak" is itself a tell that this is a non-ag TOU schedule — `touBucketForLabel` in the ag lever explicitly returns `null` for "Super Off-Peak" because it is not an ag-card bucket.
- **WHY this meter is on B-1:** because it is a **building / office**, not a pump or an ag load. PG&E rate eligibility is by end-use and service character; an office takes a commercial general-service rate (B-1 here), and a pump takes an ag schedule (AG-A/B/C). There is no question of putting this meter on an ag rate — it is not ag. The right *cross-rate* question for an office is "B-1 vs B-6/B-10/B-19 small-commercial tiers," which the ag rate engine does not and should not evaluate.

---

## (c) THE LATEST BILL fully itemized

**Latest (and only) cycle on file: 2026-02-11 → 2026-03-12, printed total $103.51.** Cycle-close (serial-code calendar date) is **2026-03-26**. Straddles the 2026-03-01 rate change, so the customer charge prints in two dated sub-periods. `coverageState: reconciled`.

| Line | Quantity | Rate | Amount |
|---|---|---|---|
| Peak energy | 48.204 kWh | $0.40252/kWh | **$19.27** |
| Off-Peak energy | 177.703 kWh | $0.38640/kWh | **$68.32** |
| Super Off-Peak energy | 16.514 kWh | $0.36291/kWh | **$5.99** |
| Customer Charge 02/11/2026 → 02/28/2026 | 18 days @ $0.32854/day | | **$5.91** |
| Customer Charge 03/01/2026 → 03/12/2026 | 12 days @ $0.32854/day | | **$3.94** |
| Energy Commission Tax | — | — | **$0.08** |
| **Printed total** | | | **$103.51** |

Arithmetic check: energy $19.27 + $68.32 + $5.99 = **$93.58**; customer $5.91 + $3.94 = **$9.85**; + Energy Commission Tax $0.08 = **$103.51**, matches the printed total exactly. Independent re-multiply: 48.204 × $0.40252 = $19.40, 177.703 × $0.38640 = $68.66, 16.514 × $0.36291 = $5.99 (the printed line amounts $19.27 / $68.32 round slightly under the raw multiply, the usual sub-kWh rounding on the bill; the **printed line totals govern** and sum exactly to $103.51).

- **Total kWh (latest cycle):** **242.421 kWh** (`idleZeroKwh: false`) — a real, steadily-used building load, not idle. ~73% of it is Off-Peak (177.7 kWh), ~20% Peak (48.2 kWh), ~7% Super Off-Peak (16.5 kWh).
- **Peak kW / demand:** **none** (`peakKw: null`, `demandChargeUsd: null`) — B-1 carries no demand charge at this usage.
- **NBCs / NEM net:** `nemNetChargeUsd: 0.00` (no solar). The only "extra" line is the **Energy Commission Tax $0.08** (`energyCommissionTaxUsd: 0.08`), a small state surcharge that ag schedules on this account do not print.

**Cycles on file:** only **1** (`cycleCount: 1`, `billedAcrossCyclesUsd = 103.51`). No multi-cycle history in the extract.

---

## (d) SOLAR / NEM status + true-up

- **NEM enrolled: NO.** `solarFlag = false`, `nemType = null`, `solarKw = null`, `trueUpMonth = null`. There is **no solar, no NEM aggregation, and no true-up** on the office meter.
- `nem = null`, `nemTrueUpUsd = null` throughout. No import/export kWh, no monthly net rows, no true-up dollar. Every charge on this bill is straight consumption + the fixed customer charge + the tiny tax. The office is a pure consumer.

---

## (e) EVERY applicable savings finding FOR THIS METER (full arithmetic + engine)

All dollar figures below come from **deterministic pure functions in `src/lib/energy`** (no AI). The only AI in the pipeline is bill-PDF vision extraction in `src/lib/extract`. The orchestrator is `src/lib/recommendations/run.ts::runEngines`. **The dominant fact for this meter is that B-1 is NON-AGRICULTURAL, so every ag lever fails closed by design.**

1. **Rate optimization (`rate-lever.ts::rateLever`, tool `rate-optimization`)** — **NO FINDING (non-ag, fails closed twice over).**
   - First gate, in `runEngines`: the rate lever only runs when `isAg(pump.rateSchedule)` is true. `isAg` requires the schedule to start with "AG". **B-1 / `HB1` does NOT start with "AG"**, so the lever is never invoked. (It also has no intervals, which would gate it out independently.)
   - Even if forced: `mapScheduleLabel("B1 Bus Low Use", ...)` looks up token `"B1"` in `LABEL_TO_PLAN`, which contains only ag families (AGA1/AGA2/AGB.../AG5C). `"B1"` is absent → the function returns **`null`**, and `rateLever` returns `{ kind: "none", reason: "unmapped_schedule" }`. The code comment is explicit: *"Unknown and non-ag labels (B1...) return null: no finding is ever built on a guessed schedule."* This meter is the literal example named in the source.
   - This is also exactly one of the **"4 non-ag B1"** cycles the back-test band calibration in `rate-lever.ts` explicitly *excluded* when tuning the 5% tolerance (39 reconciled SAs minus 4 non-ag B1 and 1 NEM-credit cycle = 34 testable). So this SA is a documented, intended exclusion, not an oversight.
   - **Determinism:** pure. **HAVE:** B-1 label, one reconciled cycle, energy by TOU bucket. **NEED (and CANNOT use the ag engine for):** the office's cross-rate analysis belongs to a **commercial rate engine** (B-1 vs other small-commercial tiers), which this product's ag-only card cannot price. **No finding from the ag levers, correctly.**

2. **Demand-charge exposure (`retrospective.ts::retrospective`, tool `demand-charge`)** — **NO FINDING.**
   - Gated on `intervals.length > 0 && pump.solarKw === null`. The office has **no intervals**, so the lever never runs. Moreover B-1 carries **no demand charge at all** (`peakKw: null`, `demandChargeUsd: null`) — there is no demand to optimize. **Determinism:** pure. **HAVE:** nothing demand-related. **NEED:** n/a (no demand exposure exists). **No finding.**

3. **Bill audit (`bill-audit.ts::billAudit`, tool `bill-audit`)** — **NO FINDING (single cycle, no comparators).**
   - The bill audit runs on every metered SA with bills, regardless of rate (it never re-prices the bill; it compares the farmer's own cycles to each other). The office bill has **no peak** (`peakKw: null`), so it would take the **no-peak path** (`noPeakBillTolerance = 0.5`, "watch" severity). That path still requires `minComparators = 3` same-season peer cycles without a peak. **The office has exactly one cycle** — zero peers — so `peers.length (0) < 3` and the engine `continue`s. **No flag.**
   - **Determinism:** pure median math, no AI. **HAVE:** one printed total ($103.51). **NEED:** ≥3 more same-season billed cycles to form a stable median before any anomaly could surface. **No finding.**

4. **Solar / NEM checks (`solar-nem.ts` / `run-solar-insight.ts`, tool `solar`)** — **NOT APPLICABLE.**
   - Both solar engines require `solarKw !== null`. The office is non-solar (`solarKw === null`, `solarFlag === false`), so neither touches it. **No finding, by construction.**

**Net for SA 4699664985:** **no >$500/yr deterministic finding, and none expected.** It is the farm office on a non-ag commercial rate (B-1), billed $103.51 for the one cycle on file, reconciling exactly. Every ag savings lever fails closed **correctly** — the rate engine literally names B-1 as the non-ag label it must refuse. The product's honest job for this meter is **legibility, not a savings claim**: show the operator "this is your KAMM office meter, here is what it costs, it is on the small-commercial B-1 rate, no demand charge, no solar." A genuine optimization (B-1 vs other commercial tiers) would need a **commercial rate engine** this ag-focused tool does not include.

---

## (f) Plain-operator-English one-liner (what the farmer sees)

> This is your Kamm office meter, not a pump — it ran 242 kWh and cost $103.51 this cycle on the small-business B-1 rate with no demand charge, and our pump rate-savings checks don't apply to it because it's an office, not an irrigation account.

---

### Provenance / caveats
- Source files: `normalized/by-meter/4699664985.json`, `normalized/manifest.json` (idx 160), engines `src/lib/energy/*`, runner `src/lib/recommendations/run.ts`. The ag rate card `fixtures/pge-ag-rate-card.json` does **not** cover B-1 (it is agricultural-only), which is why B-1 energy prices here are read off the printed bill.
- This meter is an **office/building service, not a pump** (no pump id, no GPM, no crop, no status) and is **non-solar**.
- It is one of the documented **"4 non-ag B1"** cycles excluded from the rate-lever back-test calibration — an intended exclusion, not a gap.
- All savings math is **deterministic** (`src/lib/energy`); the only AI is bill-PDF vision extraction (`src/lib/extract`). The ag levers fail closed on this non-ag meter by design.
