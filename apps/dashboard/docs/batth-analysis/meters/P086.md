# P086 (HOME RANCH) -- metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 18, `normalized/manifest.json` (serviceId 6416446454).
> **Billed = false.** This SA is **not** on billing account 4699664587-8, so **no bill PDF was extracted for it.** Everything below is from the master inventory metadata; no dollar figures are computed because there is no billed usage in hand. This is a lighter, metadata-only dossier.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P086** |
| Descriptor | PUMP 86 |
| PG&E Service Agreement (SA) ID | **6416446454** |
| Meter serial # | **1010047815** |
| Billing entity | **BATTH,CHARANJIT S** |
| PG&E account # | **6413523340-7** |
| Ranch | HOME RANCH |
| Crop served | Not recorded |
| Irrigation source | Not recorded |
| Pump capacity | **3,000 GPM** |
| Installed / drilled | 2013 |
| Status (grower's own flag) | NEW WELL |
| Latitude / Longitude | **36.517999, -119.890329** |
| Legacy flag | true (legacy-rate meter) |

This is grower pump **P086** on the HOME RANCH ranch, rated 3,000 GPM. Grower status flag: **NEW WELL**.

---

## (b) RATE SCHEDULE -- decoded

**Stored schedule: `AG5C` -- AG-5C Large Time-of-Use Agricultural Power, high-use (legacy).**

- **Legacy** large TOU ag schedule (AG-5 family), **closed since 3/1/2020**, **expires 2027**. The **C** is the >=35 hp high-use class (Rate C/F) with three summer TOU periods.
- **Charge structure:** a high daily customer charge (**$5.30871/meter/day**, ~$161/mo), the cheapest AG-5 energy, a **Max Demand charge ($14.90/kW summer & winter)**, a **Max PEAK Demand charge ($14.57/kW)** on the **legacy noon-6pm peak**, and a Max Part-Peak Demand charge ($12.59 summer / $3.51 winter). Summer = May-Oct.
- **Why it survives:** solar-legacy-TOU; parked here to keep the midday peak overlapping solar. Force-transitions to **AG-C** (late 5-8pm peak) when legacy eligibility lapses by 2027.

---

## (c) SOLAR / NEM status + true-up

- **Solar-paired.** `solarFlag = true`, solar group **4444**, NEM type **NEM2AA**, true-up month **not recorded on this row**. Per-meter nameplate kW not stored on this row (`solarKw = null`); array size lives on the farm-level solar inventory.
- **NEM2AA = NEM2 Aggregation (NEMA / NEM2A).** One array's exported generation is shared pro-rata across many load meters in a contiguous arrangement. Each benefiting meter is treated like a NEM2 account until the annual true-up. Credit is split by cumulative usage, so if total arrangement load exceeds total generation, every meter is under-credited and can owe at true-up.
- Because there is **no bill in hand** for this meter, the printed annual import/export, the true-up dollar amount, and the **Generation Allocation Summary** (which SA gets what % of the array's credit) are all unknown. The solar/NEM economics cannot be quantified from inventory metadata alone.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** All savings dollars in the product are computed by **deterministic pure functions in `src/lib/energy`** (no AI); the only AI is the bill-PDF **vision extraction** in `src/lib/extract`. With only inventory metadata, every lever is blocked at the source. To turn this metadata row into defensible dollars we need, in priority order:

1. **The account bill PDF for 6413523340-7** (entity BATTH,CHARANJIT S). The vision extractor reads the printed tariff, the billed kWh, the demand (kW), the customer/demand/energy line items, and the NEM annual true-up + import/export totals. This is the single highest-value pull -- it lights up the bill-audit lever and confirms the rate code.
2. **A UtilityAPI pull on this SA (6416446454)** to retrieve **15-minute interval kWh**. Per the standing caveat, **rate optimization is only trustworthy with interval data** -- bill summaries carry no interval kWh, and any AG-C -> AG-A1/A2 "savings" the engine emits without intervals are **sign-ambiguous artifacts that must not be quoted**. Intervals give the true max 15-min demand, which is the entire AG-C/AG-A break-even test (is max demand comfortably below 35 kW across the trailing 12 months?).
3. **The Generation Allocation Summary (Form 79-1202 roster)** for solar group 4444. It resolves which SAs are in the aggregation and each one's % share of the array credit -- without it, no solar/true-up number can be presented (arrays may be oversubscribed = zero-sum, so a true-up balance is not automatically recoverable money).

**Specific levers, and what each needs here:**

- **Rate optimization (lever 1).** AG5C is a demand-charge class; the suspicion is it may be stranded above its real demand. NEED: trailing-12-month 15-min interval kWh to compute true max demand and back-test an AG-A1/A2 switch within the +/-5% band. Cannot quote a number without intervals.
- **Bill audit (lever, own-bill anomaly).** Needs >=3 comparable same-season billed cycles (totals + peaks). We have **zero** billed cycles for this SA. NEED: the account bill PDFs.
- **Demand-response enrollment (lever 2).** Needs the demand (kW) profile to size a PDP/CBP/BIP credit. NEED: intervals.
- **Solar / NEM (lever 4).** Needs the printed true-up + the Generation Allocation Summary (above). NEED: the bill PDF + the allocation roster.

---

## (e) One-line plain-operator-English summary

> "P086 (HOME RANCH) is on account 6413523340-7, which we don't have a bill for yet, so all we know is it's on AG5C and solar-paired -- to find any dollars on it we need PG&E's bill PDF for that account plus a meter pull for the 15-minute usage."
