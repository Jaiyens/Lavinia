# 1903286356 — (no descriptor) — metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 137, `normalized/manifest.json` (Excel inventory).
> **Billed = false.** This meter is NOT on the master billed account 4699664587-8, so there is **no bill PDF in hand** — this is a
> **metadata-only** dossier. No itemized charges, no reconciled cycle, no engine-quoted dollar is possible until a bill or interval pull lands (see section d).

---

## (a) Identity

| Field | Value |
|---|---|
| Grower pump id | **none** (`growerPumpId: null`) |
| Descriptor | not recorded |
| PG&E Service Agreement (SA) ID | **1903286356** |
| Meter serial # | **1006640744** |
| Legal entity (billing name) | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account # | **1909940814-8** |
| Ranch | not recorded (`ranch: null`) |
| Crop served | not recorded (`crop: null`) |
| Irrigation source | not recorded |
| Pump capacity (GPM) | not recorded (`gpm: null`) |
| Horsepower | not recorded in the inventory (HP column blank) |
| Status | not recorded (`status: null`) |
| Latitude / Longitude | **36.271969, -119.790182** |
| Inventory rowNumber | 137 |
| Stored rate schedule | `HAGA1` |

This SA has **no grower pump id** and no GPM, so it is keyed by its **Service ID 1903286356**. On its rate code (`HAGA1`) it reads as an ag service whose pump number was not carried into this inventory row. Its coordinates (36.271969, -119.790182) sit well **south/west of the main Kerman-area ranches**, consistent with the Huron / Westlands holdings.

---

## (b) Rate schedule, decoded

**Stored schedule: `HAGA1`** — family **AG-A**.

- **What it is.** (Stored as `HAGA1`; the leading "H" is PG&E's internal handle/voltage form of `AGA1`.) AG-A1 is PG&E's **current small-agricultural** time-of-use rate for services that draw **under 35 kW**. AG-A1 is the low-use / low-demand variant and AG-A2 the high-use variant; both have a daily customer charge, peak / off-peak energy, and a demand charge that is small relative to AG-C.
- **Demand component.** This schedule carries a **modest demand charge** (AG-A1 is the low-demand sibling).
- **Size tier.** small (AG-A is PG&E's <35 kW agricultural family).
- **In the rate engine.** AG-A/B/C are the current target schedules the rate lever can quote toward (it back-tests the meter's own billed usage on the dated card, ±5% band, fail-closed). With NO bill in hand for this meter, the lever has no usage to back-test, so it stays silent until a bill PDF or UtilityAPI pull lands. The AG-B vs AG-C choice in particular needs **15-minute interval kWh** to be trustworthy.

---

## (c) Solar / NEM status + true-up

**No solar / NEM on this meter.** `solarFlag: false`, `nemType: null`, no solar group, no true-up month. This is a straight grid-served meter. Nothing to allocate and no true-up to chase.

---

## (d) What data would unlock dollar findings

**This meter has no bill in hand.** Every dollar figure in this product is produced by **deterministic pure functions in `src/lib/energy`** (the rate lever, bill audit, solar/NEM checks) — *no AI*. The only AI in the pipeline is the **bill-PDF vision extraction in `src/lib/extract`** that turns a bill image into the JSON those functions read. With metadata only, none of those engines can run for this SA. To unlock findings:

1. **The account bill PDF for 1909940814-8** (entity K S BATTH & G S BATTH PARTNERSHIP). Vision extraction (`src/lib/extract`) reads the printed tariff name, the itemized charges, the customer/demand/TOU lines, and the cycle close. That alone lets `bill-verify.ts` reconcile a cycle and `bill-audit.ts` flag anomalies. **OR**
2. **A UtilityAPI pull for SA 1903286356** (the durable Batth authorization can re-pull this SA). This is the only path to **15-minute interval kWh**, which the rate-optimization lever needs to be trustworthy — bill summaries carry no interval kWh, so any AG-B vs AG-C comparison done without intervals is **sign-ambiguous** and must not be quoted.

**Until one of the above lands, the honest output for this SA is silence (no dollar) — exactly the fail-closed behavior the engines are built for.**

---

## (e) Notable (>$500/yr) flag

**FLAG: none from metadata alone.** small/metadata-only meter with no bill; nothing clears the >$500/yr bar from metadata alone.

> A bill PDF could still surface a small reconciliation or mis-rate item, but nothing in the metadata suggests material money here.

---

## (f) One-line plain-operator-English summary

> meter 1903286356 is a meter on a different account with **no bill in hand**, so there's nothing to total up yet — we'd need its bill before any dollar is real.

**Data state:** metadata-only (Excel inventory). Billed = false. No reconciled cycle, no engine-quoted dollar.
