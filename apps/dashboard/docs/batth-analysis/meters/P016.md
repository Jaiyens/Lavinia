# P016 (PUMP # 16) — HURON — metadata-only dossier

> Source: `normalized/by-meter/6535738251.json` (primary), `normalized/inventory.json` row 28, `normalized/manifest.json` idx 27.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P016** |
| Descriptor | PUMP # 16 |
| PG&E Service Agreement (SA) ID | **6535738251** |
| Meter serial # | **1010027133** |
| Billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account # | **6539944461-4** |
| Ranch | HURON |
| Crop served | PISTACHIO |
| Irrigation source | WESTLANDS |
| Pump capacity (GPM) | **1,500 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | GOOD |
| Installed / drilled | not recorded |
| Latitude / Longitude | **36.510553, -119.944044** |
| Inventory row | 28 |

This SA is a 1,500 GPM pump, serving pistachio, on the HURON ranch, with WESTLANDS as the surface-water source. Billing entity **KANWARJIT BATTH & GAGANDIP BATTH** on account **6539944461-4**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `AG5C`.** Family: **AG-5** (LEGACY).

- **Plain English.** Legacy AG-5, small size class (C). AG-5 is a retired agricultural TOU rate that predates AG-A/AG-B/AG-C. The trailing C marks the small (<35 kW) tier. Like all AG-5 it carries TOU energy, a customer charge, and a summer demand charge.
- **Nearest alternative schedules.** AG-A1 / AG-A2 (current small ag schedules)
- **What the rate lever does here.** Legacy AG-5 is a prime rate-review candidate. The lever maps AG5C to {family:'AG-5', sizeClass:'small', realTier:'small', legacy:true}. No quoted dollar is possible without the meter's billed usage to back-test against.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** Solar group label **5219** (a grouping/array tag, not a per-meter nameplate). Per-meter kW is null on this row; true array size lives on the farm-level solar inventory (the two real arrays total 1,932 kW).
- **NEM arrangement:** **enrolled** — NEM2 Aggregation (NEM2AA): the meter's generation credits are aggregated across multiple benefiting accounts/meters on the farm, not netted only against its own load.
- **True-up month:** not recorded on this row.

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 6539944461-4** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **6535738251**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a legacy AG-5 -> current AG schedule switch dollar. Until then it is silent (or at most a qualitative 'legacy, review me' flag).
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "P016 (pistachio, HURON) runs on **AG5C** under account 6539944461-4, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 6539944461-4 bill PDF or a UtilityAPI feed for SA 6535738251 (it is a legacy AG-5 meter worth a rate review), plus the solar Generation Allocation Summary, and the levers can run."
