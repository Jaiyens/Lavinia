# Meter Dossier: SA 4699142630 (no grower pump id) — HAGC, account 4699664587-8

> Slice: billed meters, position-among-billed 35 (idx 148). Second of two meters in this slice.
> This meter has **no grower pump id** and **no ranch/descriptor** in the Excel inventory — it is one of the "extra" service IDs on the master account. The dossier is filed under its SA ID.

---

## (a) IDENTITY

| Field | Value | Source |
|---|---|---|
| PG&E SA ID (service ID) | **4699142630** | manifest + inventory + billing |
| Meter serial # | **1010760843** | inventory.meterSerial / billing.meterNumber |
| Grower pump id | **(none — null)** | inventory.growerPumpId = null |
| Descriptor | **(none — null)** | inventory.descriptor = null |
| Billing entity | **BATTH, CHARANJIT S** | inventory.billingName |
| Actual owner | (not recorded) | inventory.actualOwner = null |
| PG&E account # | **4699664587-8** (the ~46-SA master account) | inventory.account |
| Ranch | **(none — null)** | inventory.ranch = null |
| Crop | (not recorded) | inventory.crop = null |
| GPM | **(not recorded — null)** | inventory.gpm = null |
| HP | (not recorded) | — |
| Status | **(none — null)** | inventory.status = null |
| Latitude / Longitude | **36.513796, -119.886742** (has coordinates) | inventory.latitude/longitude |
| Solar flag | **FALSE** (no solar group) | inventory.solarFlag = false |
| NEM type | **(none — null)** | inventory.nemType = null |
| True-up month | **(none — null)** | inventory.trueUpMonth = null |

Identity note: this is a bare metered service on the Batth master account (4699664587-8) with no grower pump label, no ranch, no GPM, and no status. It has valid coordinates (36.5138, -119.8867 — south of the P038 cluster), so it is a real located service point, just not yet mapped by the grower to a named pump. It is **not** solar-paired (solarFlag false, nemType null) — important, because it means the solar/NEM engines are entirely out of scope here.

---

## (b) RATE SCHEDULE decoded in plain English

- **Inventory rate code:** `HAGC`. The **"H"** prefix is PG&E's marker for the **High-Use** variant of the schedule; `HAGC` resolves to the **AG-C High Use** family. The billing engine prints the tariff as **"AGC Ag35+ kW High Use"** — i.e. the same large-ag TOU schedule as P038.
- **What AG-C (High Use) is, in operator English:** the large (**35 kW+**) agricultural time-of-use rate. It carries a daily customer charge, time-of-use energy by kWh, and a monthly maximum-demand charge in $/kW. The "High Use" designation is the ag tier for services that draw substantial load (vs the low-use AG-A family).
- **Customer charge:** the bill prints **$1.43343 per day** (card AG-C = $1.43343/day ≈ $43.60/month). The single billed cycle on file is exactly `days × $1.43343`.
- **Max-demand $/kW:** the card models AG-C max-demand at **$24.95/kW** (post-2026-03-01), with the published **Demand Charge Limiter $0.50/kWh** and a summer peak-period adder. **This meter billed no demand charge at all** in the cycle on file (peakKw null, demandChargeUsd null) — it moved no load.
- **TOU windows:** AG RATE peak **5–8pm year-round** (distinct from the 4–9pm DR window). The single cycle's TOU rollup printed **Peak 0.0 kWh @ $0 and Off-Peak 0.0 kWh @ $0** — confirming zero energy moved. Card AG-C winter energy: peak $0.18430, partial-peak $0.17, off-peak $0.15861 per kWh (none applied here, 0 kWh).
- **WHY this meter is on AG-C / HAGC:** it is provisioned as a 35 kW+ ag service. Whether the High-Use AG-C tier is the *cheapest* eligible schedule for this meter cannot be judged from one zero-usage winter cycle — and the engine refuses to guess (see e). The rate is structurally plausible for a large ag SA; there is no evidence it is mis-rated, and no evidence it is correctly rated either, because there is no load to test against.

---

## (c) THE LATEST BILL fully itemized

**Only cycle on file (and the latest): 2026-02-11 → 2026-03-12, cycle close 2026-03-26, printed total $43.00.** This is the single most legible bill in this slice — `coverageState: "reconciled"` (it ties out exactly).

| Line | Quantity | Rate | Amount |
|---|---|---|---|
| Peak energy | 0 kWh | $0/kWh | **$0.00** |
| Off-Peak energy | 0 kWh | $0/kWh | **$0.00** |
| Customer Charge 02/11/2026–02/28/2026 | 18 days | $1.43343/day | **$25.80** |
| Customer Charge 03/01/2026–03/12/2026 | 12 days | $1.43343/day | **$17.20** |
| Demand charge | (none billed) | — | **$0.00** |
| NBCs / taxes / NEM | (none) | — | **$0.00** |
| **Printed total** | | | **$43.00** |

Arithmetic check: 18 × $1.43343 = **$25.80**, 12 × $1.43343 = **$17.20**, sum = **$43.00**. Matches the printed total exactly. `billedAcrossCyclesUsd = $43.00` (one cycle).

- **Peak kW:** null (no demand line). **Total kWh: 0** (`idleZeroKwh: true`). **Demand charge: null/$0.**
- This is a **pure standby meter for the period billed**: the entire $43.00 is the fixed daily customer charge prorated across the 30-day straddle of the 2026-03-01 rate change. No energy, no demand, no NEM. It is the cleanest possible "all you paid was the meter's rent" bill.

There is only one cycle on file, so there is no multi-cycle history table for this meter.

---

## (d) SOLAR / NEM status + true-up

- **No solar. No NEM.** `solarFlag = false`, `nemType = null`, `trueUpMonth = null`, and the billing block's `nem = null`. There is no NEM section, no import/export kWh, no true-up.
- Consequently the entire solar/NEM lever family (`solarNemChecks`, `nemDemandInsight`, `solarBillFloor`, `demandUncoveredShare`) is **out of scope** for this meter. There is nothing to allocate, track, or true up.

---

## (e) EVERY applicable savings finding FOR THIS METER (full arithmetic + engine)

All dollar figures are computed by **deterministic pure functions in `src/lib/energy`** (no AI). The only AI in the pipeline is bill-PDF vision extraction in `src/lib/extract`. Orchestrator: `src/lib/recommendations/run.ts::runEngines`.

1. **Rate optimization (`rate-lever.ts::rateLever` / `rate-compare.ts::rateOptimization`, tool `rate-optimization`)** — **NO FINDING.**
   - `runEngines` runs rate optimization only when `isAg && intervals.length > 0 && solarKw === null`. This meter is non-solar (good) but has **no interval history**, so the gate fails and the lever never runs. Even if it ran on the canonical-shape path, `rateLever` would:
     - map `AGC`/`HAGC` → the AG-C2 large plan,
     - find the one cycle has `totalKwh = 0`, so `totalKwhTested <= 0` → return **`reason: "no_usage_basis"`** for a non-legacy plan (it refuses to switch a current usage-tiered schedule on an idle winter meter — summer flips the comparison).
   - **This is the AG-C→AG-B sign-ambiguity guard in action.** Per ground truth: rate optimization needs 15-minute interval kWh to be trustworthy; bill summaries carry no kWh; an AG-C→AG-B "savings" emitted without intervals is a **sign-ambiguous artifact**. The engine deterministically suppresses it. **Determinism:** pure, no AI. **Data we HAVE:** rate label (HAGC/AGC), one reconciled $43.00 customer-charge-only cycle, coordinates. **Data we NEED:** 15-minute interval kWh AND at least one summer cycle with real load. **No finding — correctly silent.**

2. **Demand-charge exposure (`retrospective.ts::retrospective`, tool `demand-charge`)** — **NO FINDING.**
   - Gated on `intervals.length > 0 && solarKw === null`. No intervals → never runs. There is also no billed demand charge to retrospect on ($0, peakKw null). **Determinism:** pure. **HAVE:** the (empty) demand line. **NEED:** interval history. No finding.

3. **Bill audit (`bill-audit.ts::billAudit`, tool `bill-audit`)** — **NO FINDING.**
   - Runs on every pump with bills, but it needs **≥3 same-season comparator cycles** for a stable median. This meter has exactly **one** cycle on file, so `peers.length < minComparators` and the audit returns nothing. (With a peak it would take the precise "act" path; without one, the stricter "watch" no-peak path — neither is reachable with a single cycle.) **Determinism:** pure median math, no AI. **HAVE:** one printed total. **NEED:** at least 3 same-season cycles. No finding.

4. **Solar / NEM (`solar-nem.ts`, tool `solar`)** — **OUT OF SCOPE / NO FINDING.**
   - `solarKw === null` and not solar-paired, so `solarNemChecks` returns `[]` immediately and `nemDemandInsight` returns null. **No finding.**

5. **Legacy-fleet rollup (`run.ts`, tool `rate-optimization`, `review_legacy_fleet`)** — **NOT APPLICABLE.**
   - The aggregate "still on a closed rate" finding only collects meters whose family is AG-4/AG-5. AG-C is a **current** schedule, so this meter is never rolled in. No finding.

**Net for SA 4699142630:** **no deterministic finding of any size, and certainly none >$500/yr.** The meter is a clean, fully-reconciled, customer-charge-only standby service. The honest product story is legibility, not savings: every engine fails closed for lack of usage data, exactly as designed.

---

## (f) Plain-operator-English one-liner (what the farmer sees)

> This meter (account 4699664587-8, on the large-ag High-Use AG-C rate, no pump name yet) sat idle this period and you paid $43.00 — that's just the meter's monthly rent, no power used; we can't check it for rate savings until it pumps and we get interval reads.

---

### Provenance / caveats
- Source files: `normalized/by-meter/4699142630.json`, `normalized/manifest.json` (idx 148), rate card `fixtures/pge-ag-rate-card.json`, engines `src/lib/energy/*`, runner `src/lib/recommendations/run.ts`.
- This meter is **non-solar**; the 1,932 kW farm-wide solar (840 + 1,092) does not touch it.
- All savings math is **deterministic** (`src/lib/energy`); the only AI is bill-PDF vision extraction (`src/lib/extract`).
- Single reconciled cycle; bill ties out exactly to $43.00 (18d + 12d × $1.43343). No extraction discrepancy on this meter.
