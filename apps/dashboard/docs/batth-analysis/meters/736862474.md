# Meter Dossier (metadata-only) — SA 736862474 — metadata-only meter

> SA ID `736862474` · Meter `not in records` · Account `0731904574-1` · KANWARJIT BATTH & GAGANDIP BATTH
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 135; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **none (non-pump SA — no grower pump number)** |
| PG&E Service Agreement (SA) ID | **736862474** |
| Meter serial / number | **not in records** |
| Descriptor (inventory) | none recorded |
| Legal / billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account number | **0731904574-1** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **not recorded** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **not in records** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **not recorded** |
| Latitude / Longitude | **36.257756, -120.10735** |
| Inventory row | 135 |
| Solar flag | **FALSE** |
| Solar group | none |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is a **non-pump / metadata-only service agreement** (no grower pump number recorded).

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `OL1` → **OL1 / LS-2 (PG&E outdoor / customer-owned lighting — non-ag).**
- **Plain English:** `OL1` is an **outdoor-lighting** service code (PG&E's customer-owned outdoor area-lighting class, the 'OL'/LS family) — **not an agricultural pumping rate.** It bills a flat per-fixture/standing charge for security or yard lighting; there is **no pump, no irrigation, and usually no separate kWh of interest.**
- **Why it's in the inventory:** farms carry a few non-ag service agreements (lighting, telemetry, small standby) alongside the pump meters. This is one of them.
- **Demand exposure:** none. There is no rate-arbitrage or demand-charge story here; it is a tiny fixed-cost service. The only 'finding' that could ever apply is *idle/standby* (is the lighting load even needed), which still requires the bill to size.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `OL1`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`. No solar on this meter, no aggregation share, no import/export, **no annual true-up.**
- This is a plain full-retail load meter. Nothing to allocate, nothing to settle up.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `0731904574-1`** (KANWARJIT BATTH & GAGANDIP BATTH). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `736862474` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

SA 736862474 is on **OL1** (OL1 / LS-2) with no solar, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `0731904574-1` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

