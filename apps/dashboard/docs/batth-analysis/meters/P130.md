# P130 — EAST RANCH S — metadata-only dossier

> Source: `normalized/by-meter/1901313328.json` (primary), `normalized/inventory.json` row 114, `normalized/manifest.json` idx 113.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P130** |
| Descriptor | none |
| PG&E Service Agreement (SA) ID | **1901313328** |
| Meter serial # | **1009546112** |
| Billing entity | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account # | **1909940814-8** |
| Ranch | EAST RANCH S |
| Crop served | ALMONDS |
| Irrigation source | CONSOLIDATED |
| Pump capacity (GPM) | **600 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | GOOD |
| Installed / drilled | not recorded |
| Latitude / Longitude | **36.593205, -119.773259** |
| Inventory row | 114 |

This SA is a 600 GPM pump, serving almonds, on the EAST RANCH S ranch, with CONSOLIDATED as the surface-water source. Billing entity **K S BATTH & G S BATTH PARTNERSHIP** on account **1909940814-8**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `HAGA2`.** Family: **AG-A** (current).

- **Plain English.** Current AG-A2 (H = bill-print prefix). AG-A is PG&E's <35 kW small ag family; the 2 is the high-use sub-tier. No separately-stated demand charge: customer charge + TOU energy. The bill prints this as a SMALL schedule even though the card models AG-A2 at its large pricing row (rate-lever.ts realTier note).
- **Nearest alternative schedules.** AG-A1 (the low-use small sub-tier)
- **What the rate lever does here.** Current small schedule -> lever SILENT without a back-test. AG-A1 vs AG-A2 hinges on usage volume; only interval/usage data decides it.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** no (solarFlag=false).
- **Array / group:** No solar group / no per-meter solar kW recorded.
- **NEM arrangement:** No solar flag and no NEM type recorded. Treat as a non-solar load until a bill or generation summary shows otherwise.
- **True-up month:** not recorded on this row.

This meter shows **no solar/NEM tag**, so it is treated as a straight load. If a bill later shows generation, this row would need updating.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 1909940814-8** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **1901313328**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a within-family switch dollar. Until then it is silent.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **Bill audit needs >=3 same-season billed cycles** before it can flag an anomalous month; one cycle is never enough.

## (e) One-line summary

> "P130 (almonds, EAST RANCH S) runs on **HAGA2** under account 1909940814-8, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 1909940814-8 bill PDF or a UtilityAPI feed for SA 1901313328 and the levers can run."
