# Meter Dossier (metadata-only) — P079 — PUMP 79

> SA ID `6536550188` · Meter `1010027135` · Account `6539944461-4` · KANWARJIT BATTH & GAGANDIP BATTH
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 17; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P079** |
| PG&E Service Agreement (SA) ID | **6536550188** |
| Meter serial / number | **1010027135** |
| Descriptor (inventory) | `PUMP 79` |
| Legal / billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account number | **6539944461-4** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **BIG BLOCK** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **1700 gpm** |
| Installed / drilled | **2013** |
| Status (grower's flag) | **NEW WELL** |
| Latitude / Longitude | **36.528705, -119.930821** |
| Inventory row | 17 |
| Solar flag | **TRUE** |
| Solar group | `4444` |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | `NEM2AA` |
| True-up month | not recorded on this row |

This is grower pump **P079** serving on BIG BLOCK, rated **1700 gpm**, flagged **NEW WELL** by the grower.

It **is** flagged into the farm's solar/NEM aggregation (see section d). **Ground-truth reminder:** the two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `AG5C` → **AG5C / AG-5C (legacy large TOU ag, ≥35 hp, high use).**
- **Plain English:** `AG5C` is **legacy Schedule AG-5, Rate C/F** — the *high-use* large-TOU ag rate. It has the **biggest customer charge in the AG-5 family (~$5.31/day)**, three summer TOU periods, a **Max-Demand charge ($14.90/kW yr-round)**, a **Summer Peak-Demand charge ($14.57/kW, noon-6pm)**, and a Part-Peak demand charge — in exchange for the cheapest energy. Defining feature: **heavy demand charges, cheap energy.**
- **Closed legacy schedule**, **AG-5 expires 2027.** Held for solar-legacy / no-interval-meter cases.
- **Why it matters here:** like AG-5B, parked to hold the **legacy noon-6pm peak** against midday solar. On lapse (2027) it force-transitions to **AG-C** with the 5-8pm peak that no longer overlaps solar.
- **Demand exposure:** large. AG-5C only 'wins' when the meter pushes serious sustained kW; a low-load-factor meter stranded here is the classic over-priced case — but proving it needs interval kW.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `AG5C`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **YES — `NEM2AA` = NEM2 Aggregation, *benefiting (aggregated load)* meter.** This meter receives generation credit **virtually, via billing**, from a single generating array on a contiguous parcel under one customer-of-record (Special Condition 6 of NEM2).
- **Exports = 0 BY DESIGN.** A benefiting load meter correctly shows zero exports — the exports live on the *generating* meter; credit arrives as **allocated kWh** proportional to this meter's share of total arrangement usage.
- **Solar group label:** `4444` (a NEM2-aggregation legacy-TOU group; no standalone kW recorded for this benefiting meter — the array nameplate lives on the generating SA). **Reminder of ground truth:** the farm's two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW; the group labels like `4444` are aggregation-roster tags, not additional arrays.
- **True-up month:** **not recorded on this row** (`trueUpMonth: null`). The arrangement still settles annually; the true-up date lives on the generating/account record. Pull the arrangement's anniversary date to confirm.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `6539944461-4`** (KANWARJIT BATTH & GAGANDIP BATTH). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `6536550188` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.
   - **Priority note:** `AG5C` is a **demand-heavy / high-use** schedule. If this meter's true max 15-min demand is **comfortably below 35 kW** (especially in the 5-8pm window), it is a prime **demote-to-AG-A1/AG-A2** candidate worth (typically) hundreds to low-thousands $/yr. But that is **only** confirmable with the interval pull — do not assert a number from metadata.
3. **The Generation Allocation Summary + Form 79-1202 roster** for this aggregation arrangement (solar group `4444`). For any solar/NEM meter this is what proves the allocated-credit share and confirms the meter isn't orphaned (the P031 zero-credit failure mode). For an `NEMEXP`/`NEMEXPM`-tagged SA it is essential.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

P079 (BIG BLOCK) is on **AG5C** (AG5C / AG-5C) and is in the farm's solar/NEM aggregation, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `6539944461-4` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

