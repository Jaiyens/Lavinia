# P059 (ELKHORN SHOP) -- metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 30, `normalized/manifest.json` (serviceId 2707181864).
> **Billed = false.** This SA is **not** on billing account 4699664587-8, so **no bill PDF was extracted for it.** Everything below is from the master inventory metadata; no dollar figures are computed because there is no billed usage in hand. This is a lighter, metadata-only dossier.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P059** |
| Descriptor | Pump 59 |
| PG&E Service Agreement (SA) ID | **2707181864** |
| Meter serial # | **1010760961** |
| Billing entity | **BATTH FARMS INC** |
| PG&E account # | **2700884486-5** |
| Ranch | ELKHORN SHOP |
| Crop served | ALMONDS |
| Irrigation source | LIBERTY |
| Pump capacity | **800 GPM** |
| Installed / drilled | Not recorded |
| Status (grower's own flag) | OLD |
| Latitude / Longitude | **36.489337, -119.890278** |
| Legacy flag | Not recorded |

This is grower pump **P059** on the ELKHORN SHOP ranch, serving almonds, rated 800 GPM. Grower status flag: **OLD**.

---

## (b) RATE SCHEDULE -- decoded

**Stored schedule: `AGC` -- AG-C Schedule AG, 35+ kW high-use (current default).**

- **Current** Schedule AG, high-use sub-rate for installations **>=35 kW**. AG-C is the **demand-heavy** rate: it trades big fixed/demand cost for the cheapest energy.
- **Charge structure:** highest customer charge of the AG family (**$1.43343/meter/day, ~$43.58/mo**), a **Max Demand charge ($21.43/kW secondary)**, a **Summer Peak Demand charge ($29.92/kW)** on the highest 15-min demand in the **5-8pm** window (summer = Jun-Sep), the lowest AG energy ($0.21329/kWh peak-summer), and the AG-C-only **Demand Charge Rate Limiter** ($0.50/kWh blended cap).
- **The arbitrage:** AG-C only wins when the meter pushes serious kW for many hours (high load factor). PG&E defaults a meter to AG-C the moment metered demand touches 35 kW, but never demotes it back automatically. A low-load-factor pump stranded on AG-C pays ~$43.58/mo + demand charges it no longer justifies, and would pay only ~$0.69/day with **no demand charge on AG-A1**.

---

## (c) SOLAR / NEM status + true-up

- **Solar-paired.** `solarFlag = true`, solar group **5219**, NEM type **NEM2AA**, true-up month **not recorded on this row**. Per-meter nameplate kW not stored on this row (`solarKw = null`); array size lives on the farm-level solar inventory.
- **NEM2AA = NEM2 Aggregation (NEMA / NEM2A).** One array's exported generation is shared pro-rata across many load meters in a contiguous arrangement. Each benefiting meter is treated like a NEM2 account until the annual true-up. Credit is split by cumulative usage, so if total arrangement load exceeds total generation, every meter is under-credited and can owe at true-up.
- Because there is **no bill in hand** for this meter, the printed annual import/export, the true-up dollar amount, and the **Generation Allocation Summary** (which SA gets what % of the array's credit) are all unknown. The solar/NEM economics cannot be quantified from inventory metadata alone.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** All savings dollars in the product are computed by **deterministic pure functions in `src/lib/energy`** (no AI); the only AI is the bill-PDF **vision extraction** in `src/lib/extract`. With only inventory metadata, every lever is blocked at the source. To turn this metadata row into defensible dollars we need, in priority order:

1. **The account bill PDF for 2700884486-5** (entity BATTH FARMS INC). The vision extractor reads the printed tariff, the billed kWh, the demand (kW), the customer/demand/energy line items, and the NEM annual true-up + import/export totals. This is the single highest-value pull -- it lights up the bill-audit lever and confirms the rate code.
2. **A UtilityAPI pull on this SA (2707181864)** to retrieve **15-minute interval kWh**. Per the standing caveat, **rate optimization is only trustworthy with interval data** -- bill summaries carry no interval kWh, and any AG-C -> AG-A1/A2 "savings" the engine emits without intervals are **sign-ambiguous artifacts that must not be quoted**. Intervals give the true max 15-min demand, which is the entire AG-C/AG-A break-even test (is max demand comfortably below 35 kW across the trailing 12 months?).
3. **The Generation Allocation Summary (Form 79-1202 roster)** for solar group 5219. It resolves which SAs are in the aggregation and each one's % share of the array credit -- without it, no solar/true-up number can be presented (arrays may be oversubscribed = zero-sum, so a true-up balance is not automatically recoverable money).

**Specific levers, and what each needs here:**

- **Rate optimization (lever 1).** AGC is a demand-charge class; the suspicion is it may be stranded above its real demand. NEED: trailing-12-month 15-min interval kWh to compute true max demand and back-test an AG-A1/A2 switch within the +/-5% band. Cannot quote a number without intervals.
- **Bill audit (lever, own-bill anomaly).** Needs >=3 comparable same-season billed cycles (totals + peaks). We have **zero** billed cycles for this SA. NEED: the account bill PDFs.
- **Demand-response enrollment (lever 2).** Needs the demand (kW) profile to size a PDP/CBP/BIP credit. NEED: intervals.
- **Solar / NEM (lever 4).** Needs the printed true-up + the Generation Allocation Summary (above). NEED: the bill PDF + the allocation roster.

---

## (e) One-line plain-operator-English summary

> "P059 (ELKHORN SHOP, almonds) is on account 2700884486-5, which we don't have a bill for yet, so all we know is it's on AGC and solar-paired -- to find any dollars on it we need PG&E's bill PDF for that account plus a meter pull for the 15-minute usage."
