# P118 — HOME RANCH — metadata-only dossier

> Source: `normalized/by-meter/1203585402.json` (primary), `normalized/inventory.json` row 41, `normalized/manifest.json` idx 40.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P118** |
| Descriptor | none |
| PG&E Service Agreement (SA) ID | **1203585402** |
| Meter serial # | **1010785903** |
| Billing entity | **BATTH FARMS INC** |
| PG&E account # | **1202269697-0** |
| Ranch | HOME RANCH |
| Crop served | not recorded |
| Irrigation source | not recorded |
| Pump capacity (GPM) | **1,800 GPM** |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | NEW WELL |
| Installed / drilled | 2023 |
| Latitude / Longitude | **36.532304, -119.888049** |
| Inventory row | 41 |

This SA is a 1,800 GPM pump, on the HOME RANCH ranch. Billing entity **BATTH FARMS INC** on account **1202269697-0**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `HAGC`.** Family: **AG-C** (current).

- **Plain English.** Current AG-C, large (the leading H is PG&E's bill-print prefix for the high/large connected-load variant). Demand-charge large ag schedule: customer charge + TOU energy + summer peak demand ($/kW). The HAG- prefix is the dominant schedule across Batth's metadata-only meters.
- **Nearest alternative schedules.** AG-B2 (no-demand-charge large alternative)
- **What the rate lever does here.** Current schedule -> lever SILENT without a back-test. AG-C vs AG-B is a load-factor call that needs interval kWh; summary-only AG-C->AG-B deltas are sign-ambiguous and never quoted.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** On the **840 kW** array (the smaller of the two real Batth arrays). Per ground truth the two arrays total 1,932 kW (840 + 1,092).
- **NEM arrangement:** Tagged solar (solarFlag=true) but no NEM type is recorded on the row. The meter sits inside a solar group (label 840kw) but its NEM arrangement is not captured in the inventory.
- **True-up month:** not recorded on this row.
- **Grower solar note:** "new well".

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 1202269697-0** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **1203585402**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **Then the rate lever can run.** With billed cycles it can back-test the meter's own usage on the dated card and, only if it lands inside the +/-5% band, quote a within-family switch dollar. Until then it is silent.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "P118 (HOME RANCH) runs on **HAGC** under account 1202269697-0, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 1202269697-0 bill PDF or a UtilityAPI feed for SA 1203585402, plus the solar Generation Allocation Summary, and the levers can run."
