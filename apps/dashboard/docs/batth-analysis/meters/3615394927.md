# 3615394927 — (no descriptor) — metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 19, `normalized/manifest.json` (Excel inventory).
> **Billed = false.** This meter is NOT on the master billed account 4699664587-8, so there is **no bill PDF in hand** — this is a
> **metadata-only** dossier. No itemized charges, no reconciled cycle, no engine-quoted dollar is possible until a bill or interval pull lands (see section d).

---

## (a) Identity

| Field | Value |
|---|---|
| Grower pump id | **none** (`growerPumpId: null`) |
| Descriptor | not recorded |
| PG&E Service Agreement (SA) ID | **3615394927** |
| Meter serial # | **1011499660** |
| Legal entity (billing name) | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account # | **3619074535-0** |
| Ranch | not recorded (`ranch: null`) |
| Crop served | not recorded (`crop: null`) |
| Irrigation source | not recorded |
| Pump capacity (GPM) | not recorded (`gpm: null`) |
| Horsepower | not recorded in the inventory (HP column blank) |
| Status | not recorded (`status: null`) |
| Latitude / Longitude | **36.490868, -119.87342** |
| Inventory rowNumber | 19 |
| Stored rate schedule | `A1X` |

This SA has **no grower pump id** and no GPM, so it is keyed by its **Service ID 3615394927**. On its rate code (`A1X`) it reads as a **non-pump** service (solar/generation interconnect, shop, or facility load).

---

## (b) Rate schedule, decoded

**Stored schedule: `A1X`** — family **A-1**.

- **What it is.** A-1 / A-1X is PG&E's **small general-service** (non-agricultural) commercial rate, NOT an ag pump schedule. On the Batth inventory the A-1X meters are the **solar generation / NEM accounts** (each carries a NEM type and a true-up month) rather than an irrigation pump. A-1 has a daily customer charge plus flat (or simple TOU) energy and no separately stated demand charge; the meter exists mainly to interconnect a solar array and settle its annual NEM true-up.
- **Demand component.** This schedule has **no separately stated demand charge** (it is a small general-service rate).
- **In the rate engine.** The rate engine's `mapScheduleLabel` does NOT map A-1/A-1X to an ag card plan, so the rate-optimization lever is silent by design on this meter (correctly — it is not an ag pump).

---

## (c) Solar / NEM status + true-up

This meter is **solar-paired / NEM-enrolled** (`solarFlag: true`, `nemType: NEM2AG`, solar group label **4444**).

- **NEM type:** `NEM2AG` — NEM2 **aggregation generation** account: this SA is (or sits at) the generation/feed side of a NEM2 aggregation.
- **Annual NEM true-up month: August** (exact date not captured in the inventory).
- **Array nameplate:** Per-meter array nameplate is not stored on this row (`solarKw: null`); array size lives on the farm-level solar inventory.
- **Aggregation set (which sibling meters this array's credits flow to): NOT populated** in the metadata. The Generation Allocation Summary is the missing piece.

---

## (d) What data would unlock dollar findings

**This meter has no bill in hand.** Every dollar figure in this product is produced by **deterministic pure functions in `src/lib/energy`** (the rate lever, bill audit, solar/NEM checks) — *no AI*. The only AI in the pipeline is the **bill-PDF vision extraction in `src/lib/extract`** that turns a bill image into the JSON those functions read. With metadata only, none of those engines can run for this SA. To unlock findings:

1. **The account bill PDF for 3619074535-0** (entity K S BATTH & G S BATTH PARTNERSHIP). Vision extraction (`src/lib/extract`) reads the printed tariff name, the itemized charges, the customer/demand/TOU lines, and the cycle close. That alone lets `bill-verify.ts` reconcile a cycle and `bill-audit.ts` flag anomalies. **OR**
2. **A UtilityAPI pull for SA 3615394927** (the durable Batth authorization can re-pull this SA). This is the only path to **15-minute interval kWh**, which the rate-optimization lever needs to be trustworthy — bill summaries carry no interval kWh, so any AG-B vs AG-C comparison done without intervals is **sign-ambiguous** and must not be quoted.

3. **The Generation Allocation Summary** for solar group 4444. This meter is NEM-paired, so its real economics live in the **annual true-up**, not any single cycle. Without the allocation summary the aggregation set (`benefitingMeterSaIds`) is empty and the true-up cannot be trusted; the arrays may be **oversubscribed** (zero-sum across siblings), so no true-up dollar may be presented as "banked."

**Until one of the above lands, the honest output for this SA is silence (no dollar) — exactly the fail-closed behavior the engines are built for.**

---

## (e) Notable (>$500/yr) flag

**FLAG: yes — worth chasing once data lands.** solar/NEM-enrolled — annual true-up and aggregation allocation are material (true-ups on this farm run into the thousands), and the allocation summary is missing.

> No engine dollar can be quoted yet (no bill / no intervals). The flag means this SA should be **first in line** for a bill PDF or UtilityAPI pull, not that a saving is proven.

---

## (f) One-line plain-operator-English summary

> meter 3615394927 is a non-pump / solar service on a different account with **no bill in hand**, so there's nothing to total up yet — we'd need its bill and the solar allocation summary before any dollar is real.

**Data state:** metadata-only (Excel inventory). Billed = false. No reconciled cycle, no engine-quoted dollar.
