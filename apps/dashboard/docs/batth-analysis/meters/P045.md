# Meter Dossier — P045 (PUMP # 45, Cedar Ranch)

> SA ID `4699664955` · Meter `1011356262` · Account `4699664587-8` · BATTH, CHARANJIT S
> Generated from `normalized/by-meter/4699664955.json` + `normalized/meters.json`. Latest cycle 2026-02-11 → 2026-03-12.
> No notable (>$500/yr) finding — this is a tiny, idle, well-rated walnut pump.

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P045** |
| PG&E Service Agreement (SA) ID | **4699664955** |
| Meter serial / number | **1011356262** |
| Descriptor (bill address) | `CEDAR RANCH, PUMP # 45, P045` |
| Legal/billing entity | **BATTH, CHARANJIT S** |
| PG&E account number | **4699664587-8** (the ~46-SA master bill account) |
| Ranch | **CEDAR RANCH** |
| Crop | **WALNUT** |
| Irrigation type | **CONSOLIDATED** |
| Flow (GPM) | **600 gpm** (from Excel inventory) |
| Horsepower (HP) | **not in records** — no HP field for this meter |
| Status | **GOOD** (grower's own status flag) |
| Latitude / Longitude | **36.527844, -119.757903** (has coordinates; mappable) |
| Inventory row | 86 (manifest idx 85) |
| Solar flag | **FALSE** — no solar on this meter |
| NEM type | none (`nemType: null`) |
| Solar nameplate (kW) | n/a (no solar) |

This is a **non-solar** agricultural pump serving walnuts on Cedar Ranch under consolidated (shared) irrigation. (Reminder of farm ground truth: the two real arrays total **1,932 kW**, NOT 12,180 kW — but **none** of that touches P045; it is not NEM-enrolled.)

---

## (b) RATE SCHEDULE — decoded in plain English

- **Stored schedule:** `HAGA1` (inventory) / printed on the bill as **"AGA1 Ag<35 kW Low Use."**
- **Plain English:** **AG-A1** is a **current (not legacy)** PG&E agricultural schedule for **small pumps under 35 kW with low annual usage**. The "H" prefix in the stored `HAGA1` is the PG&E rate-family/handle code; the bill itself prints it plainly as **AG-A1, "Ag <35 kW Low Use."** AG-A1 has **NO demand charge** — small low-use ag meters are billed on a **flat daily customer (service) charge** plus simple **time-of-use energy** (Peak / Off-Peak) on whatever kWh they actually pull.
- **Customer charge:** **$0.68895 per day** (printed on this bill). Tiny — appropriate for a <35 kW low-use service.
- **Demand $/kW:** **none.** AG-A1 carries no demand charge (`demandChargeUsd: null`, `peakKw: null`). This is the right rate for a meter that never sets a meaningful peak.
- **TOU windows:** AG-A1 splits energy into **Peak** and **Off-Peak** buckets. On this cycle both are **0 kWh @ $0/kWh** (the meter is idle), so no energy dollars appear.
- **Why it is on this rate:** it is a **small, low-use** pump. AG-A1 is the **correct current schedule** for that profile — flat low service charge, no demand charge, simple TOU energy. **Mapping note:** the lever maps the `AGA1` token to card family **AG-A, small tier, realTier=small** (`LABEL_TO_PLAN` in `rate-lever.ts`). This meter is **not** legacy (`legacy: null`/not flagged), so there is no legacy-migration story here.

---

## (c) THE LATEST BILL — fully itemized

**Cycle:** 2026-02-11 → 2026-03-12 (cycle close 2026-03-26). Tariff: AGA1. **Printed total: $20.67.**
This cycle straddles the **2026-03-01 rate change**, so the customer charge prints as two sub-periods (the per-day rate happens to be identical on both sides here).

| # | Line item (kind) | Detail | Amount |
|---|---|---|---:|
| 1 | Peak energy (tou_energy) | **0 kWh @ $0/kWh** | **$0.00** |
| 2 | Off-Peak energy (tou_energy) | **0 kWh @ $0/kWh** | **$0.00** |
| 3 | Customer Charge (other) | 02/11/2026–02/28/2026, **18 days @ $0.68895/day** | **$12.40** |
| 4 | Customer Charge (other) | 03/01/2026–03/12/2026, **12 days @ $0.68895/day** | **$8.27** |

**Arithmetic check:** 0.00 + 0.00 + 12.40 + 8.27 = **$20.67.** ✔ Ties exactly to the printed total.

Sub-period sanity check: 18 × $0.68895 = $12.401 → **$12.40**; 12 × $0.68895 = $8.267 → **$8.27**. Both round correctly. 30 days total over the cycle.

**Subtotals (as extracted):**
- Customer charge total: **$20.67** (12.40 + 8.27)
- Demand-charge text total: **$0.00** (no demand on AG-A1)
- Energy Commission tax: **$0.00**
- NEM net charge: **$0.00** (no NEM)

**Energy (kWh):** Peak 0 kWh, Off-Peak 0 kWh → **0 total kWh** this cycle (`idleZeroKwh: true`, `trulyIdleNonNon-NEM` = `trulyIdleNonNem: true`). Annual cost on file is **$20.67** (`annualCostUsd`) — i.e. the only cycle on file IS this one; the meter is **truly idle**, drawing nothing but its standing service charge. No taxes, no NBCs, no demand.

**Coverage state:** `reconciled` — the extracted line items sum exactly to the printed total with no gaps. This is the cleanest possible bill: two service-charge lines and two zero-energy lines.

---

## (d) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`, `nem: null`, `nemTrueUpUsd: null`.
- **True-up:** **none** — there is no solar on this meter, so there is no aggregation share, no import/export, and no annual true-up.
- **Honesty flag:** `trulyIdleNonNem: true` — confirms this is a genuinely idle, non-solar service. Nothing to allocate, nothing to settle up.

---

## (e) SAVINGS FINDINGS — full arithmetic, engine attribution, have vs need

All dollar figures are computed by **deterministic pure functions in `src/lib/energy`** (no AI). The only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`.

### Finding 1 — Rate optimization — NO FINDING (correctly silent)
- **Engine:** `rateLever()` in `src/lib/energy/rate-lever.ts`.
- **What happens:** the meter is **already on a current schedule** (AG-A1, `legacy` not set). The lever's candidate set for a small-tier meter is `["AG-A1", "AG-A2"]` (`CANDIDATE_SCHEDULES.small`), but a candidate equal to the current plan is excluded (`p.schedule !== plan.schedule`). More fundamentally, the meter has **0 kWh and empty `intervals[]`**, so `totalKwhTested <= 0` triggers the **`no_usage_basis`** return for a non-legacy plan — the lever **refuses to quote a switch off a correct current schedule from an idle winter month** (summer usage would flip the tiering). Result: **`kind: "none"`, no dollar.** ✔
- **CRITICAL HONESTY CAVEAT (ground truth):** rate optimization needs **15-min interval kWh** to be trustworthy; bill summaries carry no kWh. Any AG-A1↔AG-A2 "savings" emitted without intervals is a **sign-ambiguous artifact**. With zero usage there is no honest savings to claim, and the engine correctly emits none.
- **Data we HAVE:** schedule label, one idle reconciled cycle, $0.68895/day customer rate.
- **Data we NEED:** a season of **15-min interval kWh** to even test whether AG-A1 vs AG-A2 differs for this pump's real load. On current evidence AG-A1 (low-use) looks correct.

### Finding 2 — Bill audit (one-cycle anomaly) — DOES NOT FIRE
- **Engine:** `billAudit()` in `src/lib/energy/bill-audit.ts`.
- **Why it stays silent:** only **1 cycle** on file (`cycleCount: 1`); the audit needs **≥ 3 comparable same-season cycles** (`minComparators`) to build a median. With one bill there is nothing to compare against. Even the no-peak path needs the same comparator count. → No finding. ✔
- **Data we NEED:** more billing cycles (a year of bills) to median against.

### Finding 3 — Solar / NEM findings — N/A
- **Engine:** `solarNemChecks()` / `nemDemandInsight()` / `demandUncoveredShare()` in `src/lib/energy/solar-nem.ts`.
- **Why N/A:** `solarKw === null` → `solarNemChecks()` returns `[]` on the first line; `nemDemandInsight()` requires `isSolar` and AG-C family → returns `null`. No solar, no NEM, no demand charge. Nothing to compute. ✔

### Finding 4 — Demand-response / pump efficiency — NOT APPLICABLE on this data
- No demand charge (AG-A1 has none) and zero usage this cycle, so there is no demand-response curtailment value and no kW-per-gallon efficiency signal to compute. The 600 gpm flow is on file but with **no kWh** we cannot compute energy-per-gallon. **Data we NEED:** interval kWh against the 600 gpm rating to assess pump efficiency.

**Net for P045:** **NO findings of any size.** Every engine correctly fails closed for lack of usage/comparators. This meter is small, idle, non-solar, and already on the right current low-use schedule. **No notable (>$500/yr) finding.**

---

## (f) One-line plain-operator-English summary

**Pump 45 at Cedar Ranch (walnuts, 600 gpm) is on the right small low-use rate (AG-A1, no demand charge) and ran nothing this cycle, so the whole bill was just $20.67 of daily meter charge — nothing to fix here.**
