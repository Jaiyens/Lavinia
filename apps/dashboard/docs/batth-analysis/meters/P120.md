# P120 (AIRPORT) -- metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 43, `normalized/manifest.json` (serviceId 8696448389).
> **Billed = false.** This SA is **not** on billing account 4699664587-8, so **no bill PDF was extracted for it.** Everything below is from the master inventory metadata; no dollar figures are computed because there is no billed usage in hand. This is a lighter, metadata-only dossier.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **P120** |
| Descriptor | Not recorded |
| PG&E Service Agreement (SA) ID | **8696448389** |
| Meter serial # | **1010760847** |
| Billing entity | **BATTH FARMS INC** |
| PG&E account # | **8693286367-2** |
| Ranch | AIRPORT |
| Crop served | Not recorded |
| Irrigation source | Not recorded |
| Pump capacity | **1,600 GPM** |
| Installed / drilled | 2021 |
| Status (grower's own flag) | NEW WELL |
| Latitude / Longitude | **36.532361, -119.91265** |
| Legacy flag | Not recorded |

This is grower pump **P120** on the AIRPORT ranch, rated 1,600 GPM. Grower status flag: **NEW WELL**.

---

## (b) RATE SCHEDULE -- decoded

**Stored schedule: `HAGC` -- HAGC AG-C taken at transmission (high) voltage.**

- **HAGC is not a separate tariff.** It is PG&E's billing code for **Schedule AG rate AG-C taken at transmission (high) voltage** (the H prefix = high/transmission voltage). Use the Transmission-voltage rows of the AG-C column.
- **Charge structure:** AG-C customer charge (~$43.58/mo) and AG-C energy, but the demand charges drop to the transmission rows: **Max Demand $6.45/kW** and **Summer Peak Demand $29.92/kW** (the peak-demand charge is the same across all voltages), 5-8pm summer (Jun-Sep) peak.
- **Context:** transmission service is only available where the customer is served directly off PG&E's transmission system, which is why HAGC clusters on this farm's large solar-paired pump banks (the 840 kW / 1,092 kW well groups).

---

## (c) SOLAR / NEM status + true-up

- **Solar-paired.** `solarFlag = true`, solar group **840kw**, NEM type **not captured**, true-up month **not recorded on this row**. Array nameplate on this row: **840 kW**.
- **NEM type not captured on this row.** The meter is flagged solar but the specific NEM2 / aggregation / export designation is blank, so we cannot tell whether it is a generating, benefiting, or standalone NEM2 meter without the bill or a UtilityAPI pull.
- Because there is **no bill in hand** for this meter, the printed annual import/export, the true-up dollar amount, and the **Generation Allocation Summary** (which SA gets what % of the array's credit) are all unknown. The solar/NEM economics cannot be quantified from inventory metadata alone.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** All savings dollars in the product are computed by **deterministic pure functions in `src/lib/energy`** (no AI); the only AI is the bill-PDF **vision extraction** in `src/lib/extract`. With only inventory metadata, every lever is blocked at the source. To turn this metadata row into defensible dollars we need, in priority order:

1. **The account bill PDF for 8693286367-2** (entity BATTH FARMS INC). The vision extractor reads the printed tariff, the billed kWh, the demand (kW), the customer/demand/energy line items, and the NEM annual true-up + import/export totals. This is the single highest-value pull -- it lights up the bill-audit lever and confirms the rate code.
2. **A UtilityAPI pull on this SA (8696448389)** to retrieve **15-minute interval kWh**. Per the standing caveat, **rate optimization is only trustworthy with interval data** -- bill summaries carry no interval kWh, and any AG-C -> AG-A1/A2 "savings" the engine emits without intervals are **sign-ambiguous artifacts that must not be quoted**. Intervals give the true max 15-min demand, which is the entire AG-C/AG-A break-even test (is max demand comfortably below 35 kW across the trailing 12 months?).
3. **The Generation Allocation Summary (Form 79-1202 roster)** for solar group 840kw. It resolves which SAs are in the aggregation and each one's % share of the array credit -- without it, no solar/true-up number can be presented (arrays may be oversubscribed = zero-sum, so a true-up balance is not automatically recoverable money).

**Specific levers, and what each needs here:**

- **Rate optimization (lever 1).** HAGC is a demand-charge class; the suspicion is it may be stranded above its real demand. NEED: trailing-12-month 15-min interval kWh to compute true max demand and back-test an AG-A1/A2 switch within the +/-5% band. Cannot quote a number without intervals.
- **Bill audit (lever, own-bill anomaly).** Needs >=3 comparable same-season billed cycles (totals + peaks). We have **zero** billed cycles for this SA. NEED: the account bill PDFs.
- **Demand-response enrollment (lever 2).** Needs the demand (kW) profile to size a PDP/CBP/BIP credit. NEED: intervals.
- **Solar / NEM (lever 4).** Needs the printed true-up + the Generation Allocation Summary (above). NEED: the bill PDF + the allocation roster.

---

## (e) One-line plain-operator-English summary

> "P120 (AIRPORT) is on account 8693286367-2, which we don't have a bill for yet, so all we know is it's on HAGC and solar-paired -- to find any dollars on it we need PG&E's bill PDF for that account plus a meter pull for the 15-minute usage."
