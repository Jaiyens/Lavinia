# 6116351334 -- metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 39, `normalized/manifest.json` (serviceId 6116351334).
> **Billed = false.** This SA is **not** on billing account 4699664587-8, so **no bill PDF was extracted for it.** Everything below is from the master inventory metadata; no dollar figures are computed because there is no billed usage in hand. This is a lighter, metadata-only dossier.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **Not recorded** |
| Descriptor | Not recorded |
| PG&E Service Agreement (SA) ID | **6116351334** |
| Meter serial # | **1011699460** |
| Billing entity | **Not recorded** |
| PG&E account # | **57448094630** |
| Ranch | Not recorded |
| Crop served | Not recorded |
| Irrigation source | Not recorded |
| Pump capacity | **Not recorded (blank GPM -- consistent with a non-pump / metadata-only service)** |
| Installed / drilled | Not recorded |
| Status (grower's own flag) | Not recorded |
| Latitude / Longitude | **Not recorded** (no coordinates on this row) |
| Legacy flag | Not recorded |

This row has **no grower pump id** (`growerPumpId = null`) and no GPM. Combined with the metadata pattern, this is most likely a **non-pump / service meter** (office, dehydrator, standby, or a misc service) rather than an irrigation pump, or a pump the grower never numbered.

---

## (b) RATE SCHEDULE -- decoded

**Stored schedule: `Not recorded` -- UNKNOWN -- rate schedule not recorded in the inventory.**

- This meter's `rateSchedule` field is **blank** in the master inventory, and there is **no bill in hand** to read the printed tariff. The rate cannot be decoded from the data we hold.
- **Decoding it requires** either the account bill PDF (the tariff is printed on it) or a UtilityAPI pull (the SA's rate code comes back with the account). Until then, every rate-based finding for this meter is blocked at the source.

---

## (c) SOLAR / NEM status + true-up

- **Solar-paired.** `solarFlag = true`, solar group **1092kw**, NEM type **not captured**, true-up month **not recorded on this row**. Array nameplate on this row: **1092 kW**.
- **NEM type not captured on this row.** The meter is flagged solar but the specific NEM2 / aggregation / export designation is blank, so we cannot tell whether it is a generating, benefiting, or standalone NEM2 meter without the bill or a UtilityAPI pull.
- Because there is **no bill in hand** for this meter, the printed annual import/export, the true-up dollar amount, and the **Generation Allocation Summary** (which SA gets what % of the array's credit) are all unknown. The solar/NEM economics cannot be quantified from inventory metadata alone.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** All savings dollars in the product are computed by **deterministic pure functions in `src/lib/energy`** (no AI); the only AI is the bill-PDF **vision extraction** in `src/lib/extract`. With only inventory metadata, every lever is blocked at the source. To turn this metadata row into defensible dollars we need, in priority order:

1. **The account bill PDF for 57448094630** (entity unknown). The vision extractor reads the printed tariff, the billed kWh, the demand (kW), the customer/demand/energy line items, and the NEM annual true-up + import/export totals. This is the single highest-value pull -- it lights up the bill-audit lever and confirms the rate code.
2. **A UtilityAPI pull on this SA (6116351334)** to retrieve **15-minute interval kWh**. Per the standing caveat, **rate optimization is only trustworthy with interval data** -- bill summaries carry no interval kWh, and any rate-switch "savings" the engine emits without intervals are **sign-ambiguous artifacts that must not be quoted**. Intervals give the true max 15-min demand, which is the entire AG-C/AG-A break-even test (is max demand comfortably below 35 kW across the trailing 12 months?).
3. **The Generation Allocation Summary (Form 79-1202 roster)** for solar group 1092kw. It resolves which SAs are in the aggregation and each one's % share of the array credit -- without it, no solar/true-up number can be presented (arrays may be oversubscribed = zero-sum, so a true-up balance is not automatically recoverable money).

**Specific levers, and what each needs here:**

- **Rate optimization (lever 1).** Cannot even start -- the rate code is unknown. NEED: the bill PDF to read the tariff, then interval kWh to test the switch.
- **Bill audit (lever, own-bill anomaly).** Needs >=3 comparable same-season billed cycles (totals + peaks). We have **zero** billed cycles for this SA. NEED: the account bill PDFs.
- **Demand-response enrollment (lever 2).** Needs the demand (kW) profile to size a PDP/CBP/BIP credit. NEED: intervals.
- **Solar / NEM (lever 4).** Needs the printed true-up + the Generation Allocation Summary (above). NEED: the bill PDF + the allocation roster.

---

## (e) One-line plain-operator-English summary

> "Service 6116351334 is on account 57448094630, which we don't have a bill for yet -- to find any dollars on it we need PG&E's bill PDF for that account plus a meter pull for the 15-minute usage."
