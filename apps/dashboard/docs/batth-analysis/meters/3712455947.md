# Meter Dossier (metadata-only) — SA 3712455947 — 20 HP IRRIG VINES AG4A

> SA ID `3712455947` · Meter `1009610039` · Account `3712455880-2` · BATTH,SURINDER K
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 166; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **none (non-pump SA — no grower pump number)** |
| PG&E Service Agreement (SA) ID | **3712455947** |
| Meter serial / number | **1009610039** |
| Descriptor (inventory) | `20 HP IRRIG VINES AG4A` |
| Legal / billing entity | **BATTH,SURINDER K** |
| PG&E account number | **3712455880-2** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **not recorded** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **not in records** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **not recorded** |
| Latitude / Longitude | **36.554405, -119.779545** |
| Inventory row | 166 |
| Solar flag | **FALSE** |
| Solar group | none |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is a **non-pump / metadata-only service agreement** (no grower pump number recorded).

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `HAGA1` → **HAGA1 / AG-A1 at transmission (high) voltage (<35 kW, low use).**
- **Plain English:** `HAGA1` is the billing code for **modern Schedule AG Rate AG-A1 taken at transmission voltage**. AG-A1 is the **<35 kW low-use** rate: a flat ~$0.69/day customer charge, **NO demand charge at all**, and simple Peak/Off-Peak energy (the *highest* energy rates of the four, because it recovers everything through energy).
- **This is the cheap, safe rate for a small low-utilization meter** — no exposure to the $29.92/kW peak-demand charge. It is usually the *destination* of the demote-from-AG-C sweep, not a problem rate.
- **Demand exposure:** none (AG-A1 has no demand charge). The only check is whether real usage is low enough that AG-A1's high energy rate still beats AG-A2's modest max-demand charge.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `HAGA1`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`. No solar on this meter, no aggregation share, no import/export, **no annual true-up.**
- This is a plain full-retail load meter. Nothing to allocate, nothing to settle up.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `3712455880-2`** (BATTH,SURINDER K). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `3712455947` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

SA 3712455947 is on **HAGA1** (HAGA1 / AG-A1 at transmission) with no solar, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `3712455880-2` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

