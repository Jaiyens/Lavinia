# Meter Dossier: P056 (Pump #56, Swanson)

> Generated for the Batth pilot analysis. Service ID `4699664538`, manifest idx 87, slice position 28 (billed-only, mod 23 == 5).
> Source of record: `normalized/by-meter/4699664538.json` (fallback `normalized/billing.json`).
> Dollar figures are computed by DETERMINISTIC pure functions in `src/lib/energy` (no AI). The only AI in the pipeline is bill-PDF vision extraction in `src/lib/extract`.

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P056** |
| Descriptor (bill / inventory) | PUMP # 56 |
| PG&E Service Agreement (SA) ID | **4699664538** |
| Meter serial number | **1011635558** |
| PG&E account # | **4699664587-8** (the master ~46-SA billed account) |
| Legal entity | BATTH, CHARANJIT S |
| Ranch | SWANSON |
| Crop | Not on file (inventory `crop: null`) |
| Irrigation set | Not on file |
| GPM (gallons/min) | **250** |
| HP (horsepower) | Not on file |
| Status | **GOOD** (inventory pump-health flag) |
| Latitude / Longitude | **36.506138, -119.863447** (geocoded, `hasCoordinates: true`) |
| Solar flag | **false** — no NEM, no array group, `nemType: null` |
| Solar nameplate | None (`solarKw: null`) |
| True-up month | None (not a solar meter) |
| Contiguous parcel | Not recorded |

A small (250 GPM) **non-solar** pump at Swanson, healthy, currently idle (0 kWh) for the latest winter cycle. Smaller sibling of the Swanson pump fleet; the lowest-complexity meter in this slice.

---

## (b) RATE SCHEDULE decoded in plain English

**Stored schedule (inventory): `HAGA1`. Billed schedule (latest cycle): `AGA1` — printed as "AGA1".**

- **What it is:** **AG-A1** is a **current (not legacy) PG&E agricultural schedule in the AG-A family.** AG-A is PG&E's **small (<35 kW) agricultural family.** The "1" tier is the **non-demand, energy-only** AG-A schedule: you pay a daily customer charge plus time-of-use energy, but there is **no monthly maximum-demand ($/kW) charge** (that is what AG-A2 / AG-B / AG-C add). In the engine, `LABEL_TO_PLAN.AGA1 = { family: "AG-A", sizeClass: "small", realTier: "small" }` and it is **not** `legacy`.
- **The `HAGA1` vs `AGA1` note:** Inventory stores `HAGA1` (the leading "H" is PG&E's internal prefix variant); the bill prints the schedule it actually billed under, **AGA1**. The mapping normalizes the bill's printed token, so the billing record's `AGA1` is authoritative for rate logic.
- **Customer charge:** Daily customer (service) charge of **$0.68895/day** on this bill — a very small fixed charge consistent with a small energy-only AG-A1 meter (contrast P052's $5.30871/day legacy AG-5C charge).
- **Demand $/kW:** **None.** AG-A1 carries no demand charge; the latest cycle has `demandChargeUsd: null` / `peakKw: null` and a $0.00 demand-text subtotal. This is correct — AG-A1 has no $/kW component.
- **TOU windows:** Energy is split into **Peak** and **Off-Peak** buckets (the bill's `touRollup` carries exactly those two periods, both at 0 kWh this cycle). The peak window the engine reasons about is the 5–8pm rate peak.
- **WHY it is on this rate:** It is correctly on a **current small-meter energy-only schedule.** With only 250 GPM and no demand exposure, AG-A1 (no $/kW) is a sensible fit — this is one of the meters that is *already* on a current, well-matched rate, not a mis-rated legacy meter. That distinction matters: the rate lever's value is finding the *wrong* ones; P056 looks right.

---

## (c) THE LATEST BILL — fully itemized

**Cycle: 2026-02-11 → 2026-03-12 (PG&E cycle close 2026-03-26). Printed total: $20.67.** Tariff: AGA1. Total energy: **0 kWh** (idle). Peak demand: none (AG-A1 has no demand charge). Coverage state: **`reconciled`** (the cleanest state — line items tie exactly to the printed total).

The cycle straddles the **2026-03-01 rate change**, so the customer charge prints in two sub-periods. Every printed line:

| # | Line item (as printed) | Basis | Amount |
|---|---|---|---|
| 1 | Peak (TOU energy) | 0 kWh @ $0/kWh | **$0.00** |
| 2 | Off-Peak (TOU energy) | 0 kWh @ $0/kWh | **$0.00** |
| 3 | Customer Charge 02/11/2026 → 02/28/2026 | 18 days @ $0.68895/day | **$12.40** |
| 4 | Customer Charge 03/01/2026 → 03/12/2026 | 12 days @ $0.68895/day | **$8.27** |
| | Demand charge | none (AG-A1) | $0.00 |
| | Energy Commission Tax | — | $0.00 |
| | NEM net charge | — | $0.00 (no solar) |

**Reconciliation:** $0.00 + $0.00 (energy) + $12.40 + $8.27 (customer) = **$20.67 = the printed total.** Exact — hence `coverageState: "reconciled"`. (Check the day math: 18 × $0.68895 = $12.401 → $12.40; 12 × $0.68895 = $8.267 → $8.27. Both match the printed sub-totals.)

Plain reading: **100% of this bill is the fixed daily customer charge.** The pump ran no measurable load this cycle; $20.67 is purely the cost of keeping a small idle meter connected for 30 days.

---

## (d) SOLAR / NEM status + true-up

**No solar. Not NEM-enrolled.** `solarFlag: false`, `nemType: null`, `solarKw: null`, no true-up month, and the billing record's `nem` block is `null`. There is no import/export, no true-up, and no aggregation exposure for this meter. The solar/NEM lever does not apply.

---

## (e) EVERY applicable savings finding FOR THIS METER

### Finding 1 — Rate optimization (lever 1): **no finding — meter is on a correct current schedule, fails closed on an idle cycle**

- **Engine / function:** `rateLever()` in `src/lib/energy/rate-lever.ts` (deterministic; tests `rate-lever.test.ts`).
- **Arithmetic / why no dollar:**
  1. `mapScheduleLabel("AGA1", card, null)` resolves to the **current, non-legacy** AG-A small plan. Because the plan is **not legacy**, the off-band fallback returns `kind: "none"` (a current meter that fails any gate stays *silent* — no qualitative note).
  2. The single cycle has **0 kWh of TOU energy.** The lever's explicit guard `if (!plan.legacy && totalKwhTested <= 0) return { kind: "none", reason: "no_usage_basis" }` fires immediately: **an idle winter meter's customer-charge delta is not a defensible reason to switch between current usage-tiered schedules** (summer flips it). So the lever **returns `none` / `no_usage_basis` by design.**
  3. Even if usage existed, AG-A1 → AG-A2 only helps a meter whose demand is high enough that the demand-charged tier wins; a 250 GPM small meter on the energy-only tier is plausibly already optimal.
- **Result emitted:** **No quoted dollar, no qualitative finding.** This is correct fail-closed behavior, and it is also a *positive* signal: P056 is one of the meters that already looks right.
- **Data we HAVE:** stored + billed schedule (AGA1, current/small/non-legacy), one idle reconciled cycle, customer-charge day rate.
- **Data we NEED:** **15-minute interval kWh from a summer pumping cycle** before any AG-A1↔AG-A2 comparison is trustworthy (bill summaries carry no kWh, and rate optimization is only trustworthy with intervals).

### Finding 2 — Bill audit (retrospective overcharge): **not triggered (insufficient comparators)**

- **Engine / function:** `billAudit()` in `src/lib/energy/bill-audit.ts` (deterministic; tests `bill-audit.test.ts`).
- **Why not triggered:** Only **1 cycle** on file (`cycleCount: 1`); the audit needs ≥ `minComparators` (default **3**) same-season peers to form a median. This meter also has no peak (it would take the stricter `noPeakBillTolerance` no-peak path at "watch" severity), but with one cycle there is nothing to compare. Emits nothing.
- **Data we NEED:** a full year of posted AG-A1 cycles to detect a dollars-up / usage-flat month.

### Finding 3 — Demand / solar levers: **N/A**

- AG-A1 carries no demand charge, so the demand-charge audit (`demand.ts`) and coincident-peak staggering (`coincident.ts`) have nothing to price.
- No solar, so `solarNemChecks` / `nemDemandInsight` / `demandUncoveredShare` return empty/null immediately (`solarKw === null` → `[]`).

### Notable-finding flag

**No finding on P056 reaches the $500/yr threshold.** This is a clean, correctly-rated, low-cost ($20.67/cycle), non-solar small meter. Its value to the pilot is the opposite of a savings hit: it is **evidence the legibility engine correctly identifies an already-optimal meter and stays silent rather than inventing a switch.**

---

## (f) One-line plain-operator-English summary

**"Pump 56 at Swanson (250 GPM, no solar) sat idle this cycle and cost just $20.67 — all of it the small fixed meter charge; it's already on the right current small-pump rate (AG-A1, no demand charge), so there's nothing to fix here until we see a summer's real numbers."**
