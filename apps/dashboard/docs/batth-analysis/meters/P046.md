# Meter Dossier (metadata-only) — P046 — PUMP # 46

> SA ID `3923515092` · Meter `1010411488` · Account `3922545703-3` · K S BATTH & G S BATTH PARTNERSHIP
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 29; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P046** |
| PG&E Service Agreement (SA) ID | **3923515092** |
| Meter serial / number | **1010411488** |
| Descriptor (inventory) | `PUMP # 46` |
| Legal / billing entity | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account number | **3922545703-3** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **SWANSON** |
| Crop | **WINE GRAPES** |
| Irrigation type | **LIBERTY** |
| Flow (GPM) | **250 gpm** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **GOOD** |
| Latitude / Longitude | **36.496674, -119.853986** |
| Inventory row | 29 |
| Solar flag | **TRUE** |
| Solar group | `5219` |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | `NEM2AA` |
| True-up month | not recorded on this row |

This is grower pump **P046** serving wine grapes on SWANSON, rated **250 gpm**, flagged **GOOD** by the grower.

It **is** flagged into the farm's solar/NEM aggregation (see section d). **Ground-truth reminder:** the two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW.

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `AG5B` → **AG5B / AG-5B (legacy large TOU ag, ≥35 hp, medium use).**
- **Plain English:** `AG5B` is **legacy Schedule AG-5, Rate B/E** — a *large* time-of-use agricultural rate for installs **≥35 hp**, medium load factor. It carries a **Max-Demand charge ($27.89/$20.54 per kW summer/winter)** and a **Summer Peak-Demand charge ($10.45/kW)** on the legacy **noon-6pm** peak window, with cheap generation energy.
- **Closed legacy schedule** (closed to new enrollment since 3/1/2020); **AG-5 is scheduled to expire in 2027.** It survives only for behind-the-meter solar-legacy-TOU customers (kept until 10 yr after permission-to-operate, no later than July 31, 2027) and meters without an interval meter.
- **Why it matters on this farm:** solar pumps are deliberately parked on AG-5B to hold the **legacy noon-6pm peak**, which front-loads the midday solar curve against peak. When that lapses (2027) the meter force-transitions to **AG-C** (the late 5-8pm peak no longer overlaps midday solar) — a structural cost increase to plan for.
- **Demand exposure:** real (demand-heavy legacy rate). Whether it is correctly priced needs interval kW.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `AG5B`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **YES — `NEM2AA` = NEM2 Aggregation, *benefiting (aggregated load)* meter.** This meter receives generation credit **virtually, via billing**, from a single generating array on a contiguous parcel under one customer-of-record (Special Condition 6 of NEM2).
- **Exports = 0 BY DESIGN.** A benefiting load meter correctly shows zero exports — the exports live on the *generating* meter; credit arrives as **allocated kWh** proportional to this meter's share of total arrangement usage.
- **Solar group label:** `5219` (a NEM2-aggregation legacy-TOU group; no standalone kW recorded for this benefiting meter — the array nameplate lives on the generating SA). **Reminder of ground truth:** the farm's two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW; the group labels like `5219` are aggregation-roster tags, not additional arrays.
- **True-up month:** **not recorded on this row** (`trueUpMonth: null`). The arrangement still settles annually; the true-up date lives on the generating/account record. Pull the arrangement's anniversary date to confirm.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `3922545703-3`** (K S BATTH & G S BATTH PARTNERSHIP). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `3923515092` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.
3. **The Generation Allocation Summary + Form 79-1202 roster** for this aggregation arrangement (solar group `5219`). For any solar/NEM meter this is what proves the allocated-credit share and confirms the meter isn't orphaned (the P031 zero-credit failure mode). For an `NEMEXP`/`NEMEXPM`-tagged SA it is essential.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

P046 (wine grapes, SWANSON) is on **AG5B** (AG5B / AG-5B) and is in the farm's solar/NEM aggregation, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `3922545703-3` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

