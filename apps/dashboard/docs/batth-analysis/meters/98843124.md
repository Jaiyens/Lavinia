# Meter Dossier (metadata-only) — SA 98843124 — metadata-only meter

> SA ID `98843124` · Meter `1010253054` · Account `0096005793-3` · KANWARJIT BATTH & GAGANDIP BATTH
> **METADATA-ONLY.** This SA is **not** on the itemized bill account 4699664587-8, so there is **no bill PDF in hand** — identity is from the Excel inventory (`normalized/inventory.json`, row 139; manifest `billed: false`). No dollar finding can be computed until the bill or interval data lands (see section e).

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **none (non-pump SA — no grower pump number)** |
| PG&E Service Agreement (SA) ID | **98843124** |
| Meter serial / number | **1010253054** |
| Descriptor (inventory) | none recorded |
| Legal / billing entity | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account number | **0096005793-3** (its own PG&E account number, separate from the 46-SA master bill account 4699664587-8) |
| Ranch | **not recorded** |
| Crop | **not recorded** |
| Irrigation type | **not recorded** |
| Flow (GPM) | **not in records** |
| Installed / drilled | **not in records** |
| Status (grower's flag) | **not recorded** |
| Latitude / Longitude | **36.283816, -120.134629** |
| Inventory row | 139 |
| Solar flag | **FALSE** |
| Solar group | none |
| Solar nameplate (kW) | n/a (no standalone array on this SA) |
| NEM type | none (`nemType: null`) |
| True-up month | not recorded on this row |

This is a **non-pump / metadata-only service agreement** (no grower pump number recorded).

---

## (b) RATE SCHEDULE — decoded

- **Stored schedule:** `HAGB` → **HAGB / AG-B at transmission (high) voltage (≥35 kW, medium use).**
- **Plain English:** `HAGB` is **modern Schedule AG Rate AG-B at transmission voltage**. AG-B is the ≥35 kW *medium-use* rate: a ~$0.916/day customer charge and **NO demand charge at all** (energy-only recovery, like AG-A1 but for the 35+ class), with peak-summer energy ~$0.513/kWh.
- **Why it's notable:** AG-B is the **no-demand-charge ≥35 kW rate** — a good landing spot for a large-but-low-load-factor meter that can't qualify for AG-A (because a month touched 35 kW) yet shouldn't eat AG-C's $29.92/kW peak-demand charge.
- **Demand exposure:** none (AG-B has no demand charge). Lower structural risk than AG-C; the AG-B vs AG-C call is an energy-vs-demand trade that needs interval kW.
- **Mapping caveat:** this decode is from `brief-pge-ag-rates.md` (Advice 7846-E, eff. 3/1/2026). PG&E re-prices several times a year; re-pull the tariff before billing-grade use. The grower's stored token here is `HAGB`.

---

## (c) SOLAR / NEM status + true-up

- **NEM enrolled:** **NO.** `nemType: null`, `solarFlag: false`. No solar on this meter, no aggregation share, no import/export, **no annual true-up.**
- This is a plain full-retail load meter. Nothing to allocate, nothing to settle up.

---

## (d) WHAT DATA WOULD UNLOCK A DOLLAR FINDING

This meter has **no bill in hand** (it is not on the itemized 4699664587-8 PDF). All savings dollars in Terra are computed by **deterministic pure functions in `src/lib/energy`** — never AI; the only AI in the pipeline is the bill-PDF **vision extraction** in `src/lib/extract`. With identity-only metadata, **every engine fails closed (no dollar)**. To make this meter quotable you need one of:

1. **The account bill PDF** for **account `0096005793-3`** (KANWARJIT BATTH & GAGANDIP BATTH). Run it through the vision extractor to recover the printed rate, the itemized charges, the demand (kW) lines, and (for solar) the NEM/true-up settlement. That alone lets `billAudit()` run once ≥3 comparable cycles are on file.
2. **A UtilityAPI pull** for SA `98843124` → **15-minute interval kWh.** This is the load-bearing data. Per ground truth, **rate optimization is only trustworthy with 15-min interval kWh** — bill summaries carry no kWh, and any AG-C↔AG-A 'savings' the engine emits *without* intervals are **sign-ambiguous artifacts**. Intervals let `rateLever()` compute the real max 15-min demand and test the demote-to-AG-A1/A2 arbitrage honestly.

**Until then:** `rateLever()`, `billAudit()`, `solarNemChecks()`, and the demand/efficiency checks all correctly return **no finding** for this SA — there is no usage basis, no comparator cycles, and (for solar) no allocation data. **No dollar figure should be attributed to this meter from metadata alone.**

---

## (e) One-line summary

SA 98843124 is on **HAGB** (HAGB / AG-B at transmission) with no solar, but it is **metadata-only** (not on the itemized bill), so there is no dollar finding yet — it needs the account `0096005793-3` bill PDF or a UtilityAPI 15-minute interval pull before any savings can be computed.

