# SA 6439420511 — metadata-only dossier

> Source: `normalized/by-meter/6439420511.json` (primary), `normalized/inventory.json` row 11, `normalized/manifest.json` idx 10.
> **Billed = false.** This meter is NOT on the single billed account 4699664587-8. The per-meter record carries `billing: null`, `summaries: []`, `nem: null` — there is **no bill, no usage, and no NEM settlement in hand**, only inventory metadata. This is a LIGHT dossier by design.

---

## (a) IDENTITY

| Field | Value |
| --- | --- |
| Grower pump id | **none (non-pump or unlabeled SA)** |
| Descriptor | none |
| PG&E Service Agreement (SA) ID | **6439420511** |
| Meter serial # | **1010127903** |
| Billing entity | **K S BATTH & G S BATTH PARTNERSHIP** |
| PG&E account # | **6431815493-3** |
| Ranch | not recorded |
| Crop served | not recorded |
| Irrigation source | not recorded |
| Pump capacity (GPM) | not recorded |
| Horsepower | not recorded in the inventory |
| Status (grower flag) | not recorded |
| Installed / drilled | not recorded |
| Latitude / Longitude | **36.49609, -119.8715** |
| Inventory row | 11 |

This SA has **no pump id, crop, ranch, or GPM** recorded — it is most likely a non-pump or service/meter-only agreement, identifiable only by its SA ID, serial, account, and coordinates. Billing entity **K S BATTH & G S BATTH PARTNERSHIP** on account **6431815493-3**.

---

## (b) RATE SCHEDULE — decoded

**Stored schedule: `E19P`.** Family: **E-19 (non-ag medium general / primary)** (LEGACY).

- **Plain English.** E-19 is a (now closed-to-new) medium commercial/industrial TOU rate, NOT an agricultural schedule. The trailing P denotes primary-voltage service. It carries TOU energy plus BOTH a peak-period and a maximum (facilities) demand charge. An ag meter sitting on E-19 is unusual and worth a hard look against the ag schedules.
- **Nearest alternative schedules.** B-19 (its successor) or, if the load is genuinely ag, an AG-B/AG-C reclassification review
- **What the rate lever does here.** NON-AG schedule: the rate-lever's mapScheduleLabel returns null for E19P (no finding is ever built on a guessed/non-ag schedule). Any reclassification needs a human tariff review plus the bill and interval data, not the automated ag lever.

---

## (c) SOLAR / NEM status + true-up

- **Solar flag:** **yes** (solarFlag=true).
- **Array / group:** Solar group label **4433** (a grouping/array tag, not a per-meter nameplate). Per-meter kW is null on this row; true array size lives on the farm-level solar inventory (the two real arrays total 1,932 kW).
- **NEM arrangement:** **enrolled** — NEM2 Aggregation (NEM2AG): a NEM2 aggregated generation/benefiting arrangement.
- **True-up month:** **May** (annual NEM settlement month).

Because this meter is solar/NEM-tagged but carries **no NEM settlement data** (`nem: null`), its annual import/export, net position, and true-up dollars are **unknown**. Per the verified ground truth, the two real arrays total **1,932 kW (840 + 1,092)** — never 12,180 kW — and array credits may be **oversubscribed (zero-sum)** across the aggregation, so no banked-credit or true-up dollar can be asserted for this meter without the Generation Allocation Summary.

---

## (d) WHAT DATA WOULD UNLOCK DOLLAR FINDINGS

**This meter has no bill in hand.** Every savings lever in `src/lib/energy` is a deterministic pure function (no AI), and each one **fails closed** with empty inputs — so today this meter yields **zero defensible dollars**. To change that:

1. **Get the bill.** Either the **account bill PDF for 6431815493-3** (AI vision extraction in `src/lib/extract` turns the PDF into the JSON the levers read) **or a UtilityAPI pull** for SA **6439420511**. Either one populates `billing`, `summaries`, and (if solar) `nem`.
2. **The automated ag rate lever does NOT apply.** `mapScheduleLabel` returns null for this non-ag schedule, so no automated rate dollar is ever built on it. With the bill in hand a **human tariff review** could judge whether the load is mis-classified (an ag pump sitting on a non-ag rate), but that is a manual call, not the engine's.
3. **For a trustworthy rate dollar, pull 15-minute interval kWh.** Bill summaries carry no interval kWh, and the AG-C<->AG-B deltas the engine can emit from summaries alone are **sign-ambiguous artifacts** — never quote them. Interval data is what makes the load-factor (and therefore the demand-vs-no-demand schedule choice) defensible.
4. **For the solar/NEM story, get the Generation Allocation Summary** (and the true-up statement for May). That resolves whether this meter's array is oversubscribed/zero-sum and populates the benefiting-meter set, before any true-up or banked-credit number is shown.

## (e) One-line summary

> "SA 6439420511 runs on **E19P** under account 6431815493-3, but we hold **only inventory metadata for it** — no bill, usage, or solar settlement — so there are no dollar findings yet. Pull the 6431815493-3 bill PDF or a UtilityAPI feed for SA 6439420511 (it is a non-ag E-19 (non-ag medium general / primary) meter — the automated ag lever never touches it; any reclassification is a manual tariff review), plus the solar Generation Allocation Summary, and the levers can run."
