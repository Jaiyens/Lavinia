# P147 (OFFICE) — metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 78, `normalized/manifest.json` (Excel inventory).
> **Billed = false.** This meter is NOT on the master billed account 4699664587-8, so there is **no bill PDF in hand** — this is a
> **metadata-only** dossier. No itemized charges, no reconciled cycle, no engine-quoted dollar is possible until a bill or interval pull lands (see section d).

---

## (a) Identity

| Field | Value |
|---|---|
| Grower pump id | **P147** |
| Descriptor | not recorded |
| PG&E Service Agreement (SA) ID | **4462569195** |
| Meter serial # | **1010417446** |
| Legal entity (billing name) | **BATHH FARMS INC** |
| PG&E account # | **4463803133-4** |
| Ranch | **OFFICE** |
| Crop served | not recorded (`crop: null`) |
| Irrigation source | not recorded |
| Pump capacity (GPM) | **1,800 GPM** |
| Horsepower | not recorded in the inventory (HP column blank) |
| Status | **NEW WELL** (grower's own condition flag) |
| Installed / drilled | 2022 |
| Latitude / Longitude | **36.539649, -119.894754** |
| Inventory rowNumber | 78 |
| Stored rate schedule | `HAGC` |

This is a newer-well irrigation pump on the **OFFICE** ranch, rated at **1,800 GPM**. (Note: the entity prints as "BATHH FARMS INC" — a spelling variant of **BATTH FARMS INC** in the source data.)

---

## (b) Rate schedule, decoded

**Stored schedule: `HAGC`** — family **AG-C**.

- **What it is.** (Stored as `HAGC`; the leading "H" is PG&E's internal handle/voltage form of `AGC`.) AG-C is a **current large-agricultural** rate (35 kW and up) that bills a separate **summer peak-period demand charge** ($/kW) on top of time-of-use energy. It tends to win over AG-B for pumps that can keep their evening (5-8pm) coincident demand low, and lose for pumps forced to run hard in the peak window.
- **Demand component.** This schedule carries a **summer peak-period DEMAND charge** ($/kW on the 5-8pm rate peak) on top of TOU energy.
- **Size tier.** large (AG-C is a 35+ kW family).
- **In the rate engine.** AG-A/B/C are the current target schedules the rate lever can quote toward (it back-tests the meter's own billed usage on the dated card, ±5% band, fail-closed). With NO bill in hand for this meter, the lever has no usage to back-test, so it stays silent until a bill PDF or UtilityAPI pull lands. The AG-B vs AG-C choice in particular needs **15-minute interval kWh** to be trustworthy.

---

## (c) Solar / NEM status + true-up

**No solar / NEM on this meter.** `solarFlag: false`, `nemType: null`, no solar group, no true-up month. This is a straight grid-served meter. Nothing to allocate and no true-up to chase.

---

## (d) What data would unlock dollar findings

**This meter has no bill in hand.** Every dollar figure in this product is produced by **deterministic pure functions in `src/lib/energy`** (the rate lever, bill audit, solar/NEM checks) — *no AI*. The only AI in the pipeline is the **bill-PDF vision extraction in `src/lib/extract`** that turns a bill image into the JSON those functions read. With metadata only, none of those engines can run for this SA. To unlock findings:

1. **The account bill PDF for 4463803133-4** (entity BATHH FARMS INC). Vision extraction (`src/lib/extract`) reads the printed tariff name, the itemized charges, the customer/demand/TOU lines, and the cycle close. That alone lets `bill-verify.ts` reconcile a cycle and `bill-audit.ts` flag anomalies. **OR**
2. **A UtilityAPI pull for SA 4462569195** (the durable Batth authorization can re-pull this SA). This is the only path to **15-minute interval kWh**, which the rate-optimization lever needs to be trustworthy — bill summaries carry no interval kWh, so any AG-B vs AG-C comparison done without intervals is **sign-ambiguous** and must not be quoted.

**Until one of the above lands, the honest output for this SA is silence (no dollar) — exactly the fail-closed behavior the engines are built for.**

---

## (e) Notable (>$500/yr) flag

**FLAG: yes — worth chasing once data lands.** large pump (1800 GPM) on a current AG rate — material annual spend; worth a rate back-test once usage lands.

> No engine dollar can be quoted yet (no bill / no intervals). The flag means this SA should be **first in line** for a bill PDF or UtilityAPI pull, not that a saving is proven.

---

## (f) One-line plain-operator-English summary

> P147 (OFFICE) is on the AG-C rate, but it sits on a **different PG&E account that we don't have a bill for** — so we can't show real numbers yet. Pull this meter's bill (or a 15-minute meter read) and we can back-test the rate.

**Data state:** metadata-only (Excel inventory). Billed = false. No reconciled cycle, no engine-quoted dollar.
