# P034 — PUMP # 34 (NEW RANCH) — metadata-only meter dossier

> Source: `normalized/inventory.json` rowNumber 54, `normalized/manifest.json` (Excel inventory).
> **Billed = false.** This meter is NOT on the master billed account 4699664587-8, so there is **no bill PDF in hand** — this is a
> **metadata-only** dossier. No itemized charges, no reconciled cycle, no engine-quoted dollar is possible until a bill or interval pull lands (see section d).

---

## (a) Identity

| Field | Value |
|---|---|
| Grower pump id | **P034** |
| Descriptor | **PUMP # 34** |
| PG&E Service Agreement (SA) ID | **8922820405** |
| Meter serial # | **1011474007** |
| Legal entity (billing name) | **KANWARJIT BATTH & GAGANDIP BATTH** |
| PG&E account # | **8922820273-8** |
| Ranch | **NEW RANCH** |
| Crop served | not recorded (`crop: null`) |
| Irrigation source | not recorded |
| Pump capacity (GPM) | **1,750 GPM** |
| Horsepower | not recorded in the inventory (HP column blank) |
| Status | **GOOD** (grower's own condition flag) |
| Latitude / Longitude | **36.534724, -119.971673** |
| Inventory rowNumber | 54 |
| Stored rate schedule | `AGC` |

This is a known, good-condition irrigation pump on the **NEW RANCH** ranch, rated at **1,750 GPM**.

---

## (b) Rate schedule, decoded

**Stored schedule: `AGC`** — family **AG-C**.

- **What it is.** AG-C is a **current large-agricultural** rate (35 kW and up) that bills a separate **summer peak-period demand charge** ($/kW) on top of time-of-use energy. It tends to win over AG-B for pumps that can keep their evening (5-8pm) coincident demand low, and lose for pumps forced to run hard in the peak window.
- **Demand component.** This schedule carries a **summer peak-period DEMAND charge** ($/kW on the 5-8pm rate peak) on top of TOU energy.
- **Size tier.** large (AG-C is a 35+ kW family).
- **In the rate engine.** AG-A/B/C are the current target schedules the rate lever can quote toward (it back-tests the meter's own billed usage on the dated card, ±5% band, fail-closed). With NO bill in hand for this meter, the lever has no usage to back-test, so it stays silent until a bill PDF or UtilityAPI pull lands. The AG-B vs AG-C choice in particular needs **15-minute interval kWh** to be trustworthy.

---

## (c) Solar / NEM status + true-up

This meter is **solar-paired / NEM-enrolled** (`solarFlag: true`, `nemType: NEMEXPM`, solar group label **Solar**).

- **NEM type:** `NEMEXPM` — NEM paired-export (M variant): net-metered with export credit, monthly/aggregation flavor.
- **Annual NEM true-up month: December** (exact date not captured in the inventory).
- **Array nameplate:** Per-meter array nameplate is not stored on this row (`solarKw: null`); array size lives on the farm-level solar inventory.
- **Aggregation set (which sibling meters this array's credits flow to): NOT populated** in the metadata. The Generation Allocation Summary is the missing piece.

---

## (d) What data would unlock dollar findings

**This meter has no bill in hand.** Every dollar figure in this product is produced by **deterministic pure functions in `src/lib/energy`** (the rate lever, bill audit, solar/NEM checks) — *no AI*. The only AI in the pipeline is the **bill-PDF vision extraction in `src/lib/extract`** that turns a bill image into the JSON those functions read. With metadata only, none of those engines can run for this SA. To unlock findings:

1. **The account bill PDF for 8922820273-8** (entity KANWARJIT BATTH & GAGANDIP BATTH). Vision extraction (`src/lib/extract`) reads the printed tariff name, the itemized charges, the customer/demand/TOU lines, and the cycle close. That alone lets `bill-verify.ts` reconcile a cycle and `bill-audit.ts` flag anomalies. **OR**
2. **A UtilityAPI pull for SA 8922820405** (the durable Batth authorization can re-pull this SA). This is the only path to **15-minute interval kWh**, which the rate-optimization lever needs to be trustworthy — bill summaries carry no interval kWh, so any AG-B vs AG-C comparison done without intervals is **sign-ambiguous** and must not be quoted.

3. **The Generation Allocation Summary** for solar group Solar. This meter is NEM-paired, so its real economics live in the **annual true-up**, not any single cycle. Without the allocation summary the aggregation set (`benefitingMeterSaIds`) is empty and the true-up cannot be trusted; the arrays may be **oversubscribed** (zero-sum across siblings), so no true-up dollar may be presented as "banked."

**Until one of the above lands, the honest output for this SA is silence (no dollar) — exactly the fail-closed behavior the engines are built for.**

---

## (e) Notable (>$500/yr) flag

**FLAG: yes — worth chasing once data lands.** solar/NEM-enrolled — annual true-up and aggregation allocation are material (true-ups on this farm run into the thousands), and the allocation summary is missing.

> No engine dollar can be quoted yet (no bill / no intervals). The flag means this SA should be **first in line** for a bill PDF or UtilityAPI pull, not that a saving is proven.

---

## (f) One-line plain-operator-English summary

> P034 (NEW RANCH) is on the AG-C rate with solar, but it sits on a **different PG&E account that we don't have a bill for** — so we can't show real numbers yet. Pull this meter's bill (or a 15-minute meter read) and we can back-test the rate and settle the solar true-up.

**Data state:** metadata-only (Excel inventory). Billed = false. No reconciled cycle, no engine-quoted dollar.
