# Meter Dossier — P027 (PUMP # 27, Chateau Fresno)

> SA ID `4697755484` · Meter `1010260190` · Account `4699664587-8` · BATTH, CHARANJIT S
> Generated from `normalized/by-meter/4697755484.json` + `normalized/meters.json`. Latest cycle 2026-02-11 → 2026-03-12.
> **FLAGGED:** carries a notable (>$500/yr) finding — a $2,461.49 NEM2 annual true-up to track.

---

## (a) IDENTITY

| Field | Value |
|---|---|
| Grower pump ID | **P027** |
| PG&E Service Agreement (SA) ID | **4697755484** |
| Meter serial / number | **1010260190** |
| Descriptor (bill address) | `CHATEAU FRESNO, PUMP # 27, P027` |
| Legal/billing entity | **BATTH, CHARANJIT S** |
| PG&E account number | **4699664587-8** (the ~46-SA master bill account) |
| Ranch | **CHATEAU FRESNO** |
| Crop | not on file (`crop: null`) |
| Irrigation type | not on file |
| Flow (GPM) | **500 gpm** (from Excel inventory) |
| Horsepower (HP) | **not in records** — no HP field for this meter |
| Status | **GOOD** (grower's own status flag) |
| Latitude / Longitude | **36.517967, -119.944373** (has coordinates; mappable) |
| Inventory row | 4 (manifest idx 3) |
| Solar flag | **TRUE** — solar-paired |
| Solar group label | **4433** (aggregation group, NEM2 sub-account) |
| Solar nameplate (kW) | **not on file** for this SA (`solarKw: null`); array sizing lives at the group/array level, not the meter row |
| NEM type | **NEM2AA** (NEM2 Aggregation) |
| Contiguous | true |

Note on solar nameplate: the per-meter `solarKw` is null here. The farm-level ground truth is that the two real arrays total **1,932 kW (840 + 1,092)**, NOT 12,180 kW. This meter participates in NEM2 aggregation group "4433"; its share of the array is an allocation question, not a fixed nameplate on the meter.

---

## (b) RATE SCHEDULE — decoded in plain English

- **Stored schedule:** `AG5C` (inventory) / printed on the bill as **"AG5C Large Time-of-Use Agricultural Power."**
- **Plain English:** AG-5 is a **legacy (closed) PG&E agricultural Time-of-Use schedule**. The "C" tier and "<35 kW / small" provenance mean this is the **small** AG-5 variant: a low-demand agricultural pump that PG&E bills mostly on a **flat daily customer charge** plus a **demand charge** on the highest kW it pulls, with time-of-use energy buckets (peak / partial-peak / off-peak) layered on top.
- **Customer charge:** **$5.30871 per day** (printed on this bill). Over a ~30-day cycle that is roughly **$159/cycle** — the dominant line on an idle meter.
- **Demand $/kW:** this cycle printed **Max Demand at $15.56/kW** (02/11–02/28 sub-period) and **$14.90/kW** (03/01–03/12 sub-period), plus a **Max Part-Peak demand** adder at **$3.67/kW** then **$3.51/kW**. (Two rates because the cycle straddles the 2026-03-01 rate change.)
- **TOU windows:** AG-5 TOU separates energy into peak / partial-peak / off-peak. On THIS cycle the meter pulled **0 kWh**, so no peak/partial/off-peak energy dollars appear — only the demand and customer charges.
- **Why it is on this rate:** it is a **legacy meter** (`legacy: true`). AG-5 is closed to new enrollment; this SA has simply never been migrated to a current AG-A/AG-B/AG-C schedule. Legacy AG-4/AG-5 sitting next to current AG-A1/A2/B/C is exactly the mixed-rate sprawl the product exists to surface. **Mapping note:** the lever maps the `AG5C` token to card family **AG-5, small tier, realTier=small** (`LABEL_TO_PLAN` in `rate-lever.ts`).

---

## (c) THE LATEST BILL — fully itemized

**Cycle:** 2026-02-11 → 2026-03-12 (cycle close 2026-03-26). Tariff: AG5C. **Printed total: $162.27.**
This cycle straddles the **2026-03-01 rate change**, so most charges appear twice (a pre-change 02/11–02/28 sub-period and a post-change 03/01–03/12 sub-period).

| # | Line item (kind) | Detail | Amount |
|---|---|---|---:|
| 1 | Customer Charge (other) | 02/11/2026–02/28/2026, **18 days @ $5.30871/day** | **$95.56** |
| 2 | Max Demand (demand) | 02/11–02/28, **0.160000 kW @ $15.56000/kW** | **$1.49** |
| 3 | Max Part Peak (demand) | 02/11–02/28, **0.160000 kW @ $3.67000/kW** | **$0.35** |
| 4 | Customer Charge (other) | 03/01/2026–03/12/2026, **12 days @ $5.30871/day** | **$63.70** |
| 5 | Max Part Peak (demand) | 03/01–03/12, **0.160000 kW @ $3.51000/kW** | **$0.22** |
| 6 | Max Demand (demand) | 03/01–03/12, **0.160000 kW @ $14.90000/kW** | **$0.95** |

**Arithmetic check:** 95.56 + 1.49 + 0.35 + 63.70 + 0.22 + 0.95 = **$162.27.** ✔ Ties exactly to the printed total.

**Subtotals (as extracted):**
- Customer charge total: $159.26 (95.56 + 63.70)
- Demand-charge text total: $2.44 (1.49 + 0.35 + 0.22 + 0.95 = 3.01; the extractor's "demandChargeTextUsd" rolls only part-peak+one demand sub-period to $2.44 while the structured demand field reports **$3.01** as the cycle max-demand charge — both are present, see below)
- Energy Commission tax: $0.00
- NEM net charge (this cycle): $0.00

**Demand reconciliation:** the structured demand charge for the cycle is **$3.01** at a peak of **0.16 kW**. That $3.01 = 1.49 + 0.35 + 0.95 + 0.22 (the four demand-flavored sub-period lines). The meter is **idle (0 kWh)** this cycle, so the 0.16 kW is a trivial standby draw. There are **no peak / partial-peak / off-peak ENERGY (kWh) lines** because total usage was zero.

**Energy (kWh):** Peak 0 kWh, Partial-Peak 0 kWh, Off-Peak 0 kWh → **0 total kWh** this cycle (`idleZeroKwh: true`). Annual cost across the year on file is **$2,623.76** (`annualCostUsd`), so this meter is NOT a year-round zero — it runs in season; this particular winter cycle is just idle.

**Taxes / NBCs:** none printed on this cycle beyond the $0.00 Energy Commission tax line.

---

## (d) SOLAR / NEM status + true-up

- **NEM enrolled:** **YES** — `nemType: NEM2AA` (NEM2 Aggregation), solar group **4433**.
- **Annual true-up amount on file:** **$2,461.49** (`trueUpAmountUsd`), **true-up month = May (5)**. (The Excel inventory's `trueUpMonth` is blank for this row; the billing extraction supplies month 5.)
- **Annual energy position (printed):** Import **16,949 kWh**, Export **39,855 kWh**, **Net −22,906 kWh** — i.e. this meter's aggregation share **exports far more than it imports** over the year. (A derived re-sum lands at export ≈ 45,949 kWh / net ≈ −29,000 kWh; printed and derived differ, so this is `needs_review`.)
- **Monthly NEM net (printed statements on file):**

| Period close | Net kWh | $ |
|---|---:|---:|
| 2025-06-09 | 0 | 0.00 |
| 2025-07-10 | 13,496 | 2,756.93 |
| 2025-08-10 | −12,759 | 2,698.20 |
| 2025-09-10 | 3,453 | 1,012.42 |
| 2025-10-10 | −5,980 | −930.80 |
| 2025-11-10 | −5,487 | −845.01 |
| 2025-12-11 | −1,872 | −281.19 |
| 2026-01-12 | −3,528 | −511.44 |
| 2026-02-11 | −4,135 | −580.76 |
| 2026-02-11→02-28 | −3,314.28 | −465.24 |
| 2026-03-01→03-12 | −2,779.70 | −391.62 |

- **Coverage state:** `needs_review` — printed vs derived export/net disagree, and the monthly rows mix full-cycle and split (rate-change) sub-periods, so the annual reconciliation is not clean.
- **Honesty flag:** `nemEnrolledZeroSolarBenefit: false` — this is NOT the zero-credit anomaly. (That anomaly is the **sibling P031 / VINES 75HP** $62,795.65 true-up, proven against P038; do **not** conflate P027 with it.)

---

## (e) SAVINGS FINDINGS — full arithmetic, engine attribution, have vs need

All dollar figures below are computed by **deterministic pure functions in `src/lib/energy`** (no AI). The only AI anywhere in this pipeline is the bill-PDF **vision extraction** in `src/lib/extract`.

### Finding 1 — NEM2 true-up tracking ($2,461.49, MAY) — NOTABLE (>$500/yr)
- **Engine:** `solarNemChecks()` in `src/lib/energy/solar-nem.ts`, branch (2) "NEM2 true-up tracking." Deterministic; emits a `track_trueup` recommendation at severity **info** when `nemType` and `trueUpMonth` are both present.
- **Arithmetic:** the figure is **read directly from the data**, not modeled: `trueUpAmountUsd = $2,461.49`, `trueUpMonth = 5` → "your annual solar true-up settles in **May**; the amount on file is **$2,461.49**." No multiplication, no rate assumption.
- **Why it matters:** $2,461.49 is the annual settle-up on this aggregation share — well over the $500/yr notability bar. The grower should know it lands in May so it is not a surprise.
- **Data we HAVE:** trueUpAmountUsd, trueUpMonth, full monthly NEM net rows, annual import/export/net.
- **Data we NEED:** the **Generation Allocation Summary** to confirm how the 1,932 kW of array is split across aggregation group 4433 — the monthly net export is large, which suggests this meter's allocation is generous; reconciling printed vs derived export (16,949 import / 39,855 vs 45,949 export) needs the allocation table. `coverageState: needs_review` until then.

### Finding 2 — "Solar does not cover your demand charge" — DOES NOT FIRE here
- **Engine:** `solarNemChecks()` branch (1) and the canonical `nemDemandInsight()` / `demandUncoveredShare()` in `solar-nem.ts`.
- **Why it stays silent:** branch (1) needs a demand charge set in the **4–9pm peak window** (`peakAt` in-window). This cycle's demand is **$3.01 at 0.16 kW** with no `peakAt` timestamp (no interval data), so the trigger condition is not met. `nemDemandInsight()` additionally **gates to the AG-C family only**; this meter is **AG-5**, so it returns `null` by design (fail-closed). **No false finding emitted.** ✔
- **Data we NEED to ever fire it:** 15-min interval kWh / a printed peak timestamp **and** an AG-C schedule. Neither applies.

### Finding 3 — Rate optimization (AG-5 legacy → current AG-A/B/C) — QUALITATIVE ONLY, NO DOLLAR
- **Engine:** `rateLever()` in `src/lib/energy/rate-lever.ts` (deterministic back-test gate, `BACK_TEST_BAND_PCT = 5%`).
- **What happens:** the meter is `legacy: true` on AG-5. The lever wants to back-test the meter's CURRENT charges against the dated rate card from **its own billed usage**. But this meter has **0 kWh on the latest cycle and empty `intervals[]`** — there is **no usage basis**. Per the lever's logic, a legacy meter with no testable usage returns a **`qualitative`** result (`isLegacy: true`, reason `no_testable_cycles` / `no_savings`) — a "you are on a closed legacy schedule, worth reviewing" note with **NO dollar figure**, not an estimate.
- **CRITICAL HONESTY CAVEAT (ground truth):** rate-optimization needs **15-min interval kWh** to be trustworthy; bill summaries carry no kWh. Any AG-5→AG-B/AG-C "savings" the engine would emit **without intervals are sign-ambiguous artifacts** and must NOT be surfaced as a dollar. This meter has no intervals, so the lever **correctly emits no dollar**.
- **Data we HAVE:** schedule label, the one idle winter cycle, demand kW.
- **Data we NEED:** a full season of **15-min interval kWh** (peak / partial-peak / off-peak buckets) to price candidate current schedules and pass the 5% back-test band.

### Finding 4 — Bill audit (one-cycle anomaly) — DOES NOT FIRE
- **Engine:** `billAudit()` in `src/lib/energy/bill-audit.ts`.
- **Why it stays silent:** it needs **≥ 3 comparable same-season cycles** for a stable median; only **1 cycle** is on file (`cycleCount: 1`). Fails the `minComparators` gate → no finding. ✔ Honest: cannot audit a single bill against itself.

**Net for P027:** ONE notable finding — track the **$2,461.49 May NEM true-up** (Finding 1). Everything else is correctly silent (fail-closed) for lack of interval data and comparators.

---

## (f) One-line plain-operator-English summary

**Pump 27 at Chateau Fresno is on the old AG-5 power rate and is tied into your solar; it ran nothing this cycle (bill was $162.27, almost all the daily meter charge), and its solar true-up of $2,461.49 settles up in May — that is the number to watch.**
