{
  "name": "@lavinia/extension",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 portability probe for Almond Logic. One-time cookie-forward viability check + manual backfill. NOT the production scraper, NOT CI-gated, NEVER runs in production.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
